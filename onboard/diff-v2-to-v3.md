# diff v2 → v3 — full course (Round-2 audience review applied — FINAL)

Date: 2026-08-06. Round 2 reviewers (audience personas, each with a four-part reflection + rows): Kasten Backup Admin (KBA, 5 rows), Kasten Director (KDO, 11 rows, 3 critical), Kasten VDP Implementer (KVA, 13 rows, 2 critical) — 29 rows total, merged in `review-rows-round2-all.json`, split per file, applied by per-file LXD revision turns. Every row is dispositioned below; nothing was silently dropped.

---
# diff v2 → v3 — Lesson01_From_VDP_Admin_to_Kasten_Admin.html

Round 2 reviewer rows: `_sources/rows-round2-lesson01.json` (2 rows: 1 Kasten Backup Admin, 1 Kasten Director).
Gates after revision: lint-voice 0 errors / 1 warning (pre-existing empty `alt` on the lightbox `<img>`, kit-owned) · smoke-test-html pass (same pre-existing component-missing/headless-skipped notices as v2, nothing new) · verify-quotes 21/21 quotes found in source.

## Row dispositions

- **KBA-v2-001 — ADJUSTED (verified, no new citation needed).** Section 01, first compare card, "Where does it live?" row. The reviewer is right that "namespace" is used repeatedly through this module (this row, the note--key box's "namespace-scoped application," the concept map's five references, Section 05's mentions) without the on-the-spot definition or explicit Module 02 deferral the hero promises every Kubernetes term gets — the exact courtesy the module already extends to "Helm" two paragraphs earlier. Applied the reviewer's inline gloss reusing the folder analogy, but tightened the syntax: the proposed wording ("Inside a namespace — a named boundary on the cluster that groups... — on a cluster, alongside other applications") repeats "on a[/the] cluster" twice because the gloss and the sentence tail both carry it. Moved "on the cluster" into the gloss itself and dropped the duplicate from the tail, so it now reads "Inside a namespace — a named boundary that groups an application's pieces together on the cluster, roughly the way a folder groups related VMs in your inventory (Module 02 covers it properly) — alongside other applications." No new `[Src:]` needed: like the Helm gloss applied in the v1→v2 pass (KS-v1-003), this defines general Kubernetes vocabulary, not a Veeam Kasten product behavior, and I checked the 9.0.2 guide directly (pp.346–349) — it uses "namespace" constantly but never defines the term itself, confirming this is course-level scaffolding, not a citable product claim. The existing `[Src: Kasten Docs 9.0.2 p.347]` on "Namespace selection is how most policies choose what to protect" is untouched and still anchors the one claim in the sentence that needed it.

- **KDO-v2-010 — ADJUSTED (verified citation + naming fix).** Section 05 "For a customer conversation" sales aside. The reviewer's diagnosis is correct: "its own audit story" is reassurance-flavored language the module never backs with a mechanism, while the very next paragraph in this same section already establishes a concrete, citable fact (the catalog identifier appended to the VBR job name). Applied the reviewer's swap — dropping the unsupported "audit story" item from the three-item list down to two, and replacing the vague claim with the concrete mechanism, now cited inline as `[Src: Kasten Docs 9.0.2 p.129]`. Verified the citation myself against the text cache: p.129 reads "A Veeam Kasten catalog identifier is appended to the name to ensure uniqueness across multiple clusters that back up to the same VBR server" — supports the clause exactly as proposed. One fitting change to the reviewer's wording: the proposal said "the Kasten catalog identifier appended to its name," but the product-naming rule for this kit is "Veeam Kasten" after first use, with bare "Kasten" reserved for verbatim technical strings — and the sentence three lines above this same aside already uses the full "Veeam Kasten catalog identifier appended to the job name" phrasing, so bare "Kasten" here would also be a same-page inconsistency, not just a style-guide violation. Corrected to "the Veeam Kasten catalog identifier appended to its name." Everything else — including the reviewer's "same job-level reporting your team already relies on rather than a new audit capability" framing — applied verbatim.

## Net change

Both Round 2 rows for this file applied, in adjusted form: one for syntax (removing a duplicated "on the cluster" the reviewer's own proposal introduced), one for product-naming consistency (bare "Kasten" corrected to "Veeam Kasten" to match the kit's naming rule and the same section's existing usage three lines above). Neither adjustment changes the substance the reviewers asked for. The net effect: "namespace" now gets the on-the-spot gloss the hero's own promise requires, closing the one term this module used without defining or deferring; and the Section 05 sales aside no longer implies an audit capability the product documentation doesn't describe, instead pointing at the real, already-cited mechanism. No new `[VERIFY]` markers introduced — both new claims were verified against the 9.0.2 guide before shipping (namespace vocabulary confirmed as undefined-by-the-product-doc general K8s terminology needing no citation; the catalog-identifier claim confirmed word-for-word on p.129). Structure untouched: five sections, all signposts and Continue gates in place, exactly five knowledge-check questions, quiz and glossary popovers unchanged.

---
# diff v2 → v3 — Module 02 (Lesson02_Kubernetes_Foundations_for_Backup_Admins.html)

Round 2 reviewer rows: `_sources/rows-round2-lesson02.json` (1 row — Kasten Director, minor).
Disposition: **0 applied · 1 adjusted · 0 deferred.**

## Minor rows

- **KDO-v2-009** — **ADJUSTED.** Location: Namespaces section, dashboard screenshot figcaption. The Director's read is correct — "SLA" lands in the module as a verbatim product term without ever being unpacked, and a director-level reader really could carry "SLAs are being respected" back to their own leadership as if Veeam Kasten were reporting against an externally negotiated commitment. The gap is real and the fix belongs exactly where the row puts it.

  I did not ship the proposed sentence unchanged, though. The proposal tacks the clarifying clause onto the end of the sentence under the same `[Src: Kasten Docs 9.0.2 p.301]` citation that already covers the primary claim ("compliant... SLAs are being respected"). p.301 supports that primary claim but says nothing about what constitutes the SLA — it uses the term without defining it, same gap the Director is flagging. Citing p.301 for a clause p.301 doesn't support would trade one glossed-over term for one mis-cited claim, which is worse, not better.

  So I verified the clarifying clause against its own source before shipping it. p.382 ("Using Policy Presets"): "Operations teams can define multiple protection policy presets that specify parameters such as schedule, retention, location and infrastructure. A catalog of organizational policy presets and SLAs can be provided to the development teams..." — this is where the product ties "SLA" to policy-preset parameters (schedule, retention) rather than to any external contract. p.620 (glossary) reinforces it: "PolicyPreset... can represent organizational SLAs requiring a user to specify only the application details to be used in a Policy." Both pages confirm the Director's underlying point — a Veeam Kasten "SLA" is the policy's own configured parameters, not a negotiated external commitment — so the clarification is a verifiable claim, not just plausible-sounding filler.

  Shipped: the clause keeps the Director's exact wording ("meaning the policy's own configured schedule and retention, not a separately negotiated or externally audited service-level agreement") but carries its own citation, `[Src: Kasten Docs 9.0.2 p.382]`, placed immediately after the clause per this file's established convention of one citation per clause (see the existing p.303/p.303 and p.4/p.4 sentence pairs elsewhere in the file) rather than stacking two citations at the sentence's end. The original `[Src: Kasten Docs 9.0.2 p.301]` citation is untouched and still sits directly after "are being respected," where it belongs.

  Full sentence now reads: "...compliant when a policy applies and its service-level agreements (SLAs) are being respected [Src: Kasten Docs 9.0.2 p.301] — meaning the policy's own configured schedule and retention, not a separately negotiated or externally audited service-level agreement [Src: Kasten Docs 9.0.2 p.382]."

## Deferred

None. The single row is adjusted (re-cited, wording unchanged) and shipped.

## Net change

v3 closes the one open gap from the Round 2 audience panel: "SLA" no longer appears as an unexplained product term in the dashboard figcaption. The fix is scoped to a single sentence — one clarifying clause with its own verified citation (p.382, corroborated by the p.620 glossary entry) — and does not touch the clause's existing p.301 citation, the screenshot, the alt text, the green callout beneath it, or any other section of the file. No naming, structure, or code-block changes were needed or made.

---
# diff v2 → v3 — Lesson 03 · Deploying and Configuring Veeam Kasten

Reviewer rows: `_sources/rows-round2-lesson03.json` (16 rows — 3 critical, 5 significant, 8 minor) from the Round-2 audience panel: Kasten VDP Implementer (KVA-), Kasten Director (KDO-), Kasten Backup Admin (KBA-).

Disposition per row. **Applied** = proposal taken as written. **Adjusted** = intent applied, wording / citation / placement fitted. **Deferred** = not applied, with reason. Every new factual claim was verified against `Reference/Product Documents/K10/.text-cache-veeam_kasten_9.0.2_user_guide.txt` before it went in.

Totals: **5 applied · 11 adjusted · 0 deferred.** One `[VERIFY]` marker ships deliberately (KVA-v2-006); two `[VERIFY]` requests from reviewers (KVA-v2-002, KVA-v2-004) were resolved against the source instead of shipped.

---

## Critical

- **KVA-v2-001 — APPLIED** (and it was a real defect, not a nuance). The caption under `helm repo add` claimed the chart-repo add is "the only step in the module that talks to the internet". The guide says otherwise in its own words: for environments connected to the internet, one needs access to **three** repositories to install Veeam Kasten — the Helm repository containing the chart, the container registry containing the Veeam Kasten container images, and upstream repositories for dependencies such as Prometheus [Src: Kasten Docs 9.0.2 p.149] — and all images for a default install are hosted at `gcr.io/kasten-images` [Src: Kasten Docs 9.0.2 p.8]. Four edits landed: (1) the caption now says "this is the chart repository only — one of three places an internet-connected install reaches out to"; (2) a new **"Check this before the install window"** caution in Section 1 states the three-repository fact, the registry host, the private-registry option for air-gapped installs [Src: p.149], and the failure signature (a blocked registry path fails neither the repo-add nor the install command — it surfaces as pods that never reach Running); (3) a fourth hero prerequisite, *Image-pull reachability*; (4) a ninth, **blocking** readiness-checklist item — "The cluster nodes can pull the Veeam Kasten container images" — so the gate the implementer actually needs is in the gate list, not only in prose. The checklist lede now reads "Six items are blocking", and the verdict JS was changed from the hard-coded "all eight items confirmed" to `boxes.length`, so the widget no longer contradicts itself when items are added. Its blocking-verdict line now reads "The primer tool answers the Kubernetes, CSI and annotation items for you; the registry path is yours to confirm" — the primer's three operations [Src: p.7] do not include registry reachability, so the checklist no longer implies the tool covers it.
- **KVA-v2-002 — ADJUSTED, and the `[VERIFY]` resolved rather than shipped.** The row was right that the Veeam Repository field list had no authentication step, and the source answers the question outright: **Provide access credentials in the Username and Password fields** [Src: Kasten Docs 9.0.2 p.337]; **Ensure Access Permissions are granted within VBR for the specified account (or related security group) and backup repository** [Src: Kasten Docs 9.0.2 p.338]. Two new paragraphs in "Pointing it at a Veeam repository" carry those facts plus the submit-time behaviour the implementer will hit next: the dialog validates the input data on Submit, communication with the server uses SSL and requires the server's certificate be trusted by Veeam Kasten, and the **Skip certificate chain and hostname verification** option disables certificate validation [Src: p.338]. The destination table's Veeam Repository row also gained "plus access credentials in the Username and Password fields [Src: p.337]" so the table and the walkthrough agree. The account-preparation work is framed as a Veeam Data Platform-side pre-implementation task, which is the row's real ask — the implementer no longer has to guess whether to create a Veeam Backup & Replication account first.
- **KDO-v2-003 — ADJUSTED.** The Director's read is correct: "not advisable if granular access is needed" understates a mode with no authentication at all. The guide's clause stays verbatim (it is the source's own wording and the quiz depends on it), and the cell now opens with "No authentication method is configured" and closes with the consequence spelled out: read "all operations" literally, it includes the destructive ones, and with no authentication configured there is no authenticated identity behind any of them — treat Direct Access as lab-only, not the exposure choice on a cluster whose restore points you would have to account for. Two changes from the proposal: "including deleting a restore point" became "the destructive ones" (the guide says "all Veeam Kasten operations"; naming one specific operation the source does not name is an unnecessary inference), and "no authenticated identity to record against the action" became "no authenticated identity behind any of them" — the guide makes no statement about what is or is not recorded in an audit log, so the claim stays on what follows logically from "no authentication configuration" [Src: p.220]. Knock-on edit of my own: the Direct Access distractor rationale in knowledge-check Q3 was rewritten to match, so the quiz no longer teaches the softer framing the row objected to.

## Significant

- **KVA-v2-003 — APPLIED.** "As specified elsewhere" now points somewhere for every platform this audience runs, under a new sub-heading after the AWS block: Azure service-principal credentials [Src: p.39]; Google Cloud's two Service Accounts and the `roles/compute.storageAdmin` role [Src: p.79, p.83]; vSphere CSI credentials via Helm parameters or a vSphere Infrastructure Profile [Src: p.91]; OpenShift's `--set scc.create=true` for the SecurityContextConstraints [Src: p.55]. Two runnable additions: the guide's full Azure service-principal `helm install` command, and the vSphere credential flag trio — both quoted as exact multi-line blocks that exist verbatim in the source, so the module now has something to copy for the two most likely non-AWS cases.
- **KVA-v2-004 — ADJUSTED, `[VERIFY]` resolved.** The guide publishes no htpasswd invocation, so a `[VERIFY]` would have sent the Architect after something that is not there. What the source does specify is the *shape* of the string, which is the thing that actually goes wrong: htpasswd credentials in the format `username:hashedpassword`, generated with an online tool or the htpasswd binary found on most systems, then used to log in to the dashboard [Src: Kasten Docs 9.0.2 p.45]. The paragraph now says plainly that no invocation is published ("do not go hunting for one"), gives the format, and points at the already-quoted flag example as the form to compare output against. The secret alternative gained the missing detail: the secret must be created with the key named `auth` and the value as the password generated using htpasswd in the data field [Src: p.221].
- **KVA-v2-005 — APPLIED.** `Missing deletionPolicy, using default` now carries a verdict: read it as information, not a finding to clear. It carries no `- OK` because it reports a state rather than passing a test, and the state is acceptable — the guide calls the default deletion policy the default and recommended one [Src: p.102], and the release notes record that a `Retain` deletionPolicy for VolumeSnapshotClasses is no longer required or recommended [Src: Kasten Docs 9.0.2 p.1153]. The paragraph closes by naming the line that *is* a stop condition: the annotation line above it [Src: p.103].
- **KVA-v2-006 — ADJUSTED; the one deliberate `[VERIFY]` in this build.** "Go to the support docs" is now backed by a documented first action: a new "If pods do not reach Running" sub-section quotes the debug script the guide asks you to run as a first step to get information to support, with the archive it produces (`k10_debug_logs.tar.gz`) and the System Information page alternative [Src: Kasten Docs 9.0.2 p.862]. A "Before you tear it down" caution covers the removal path the row asked about: uninstall with the Helm command so non-namespaced resources are cleaned up (`helm uninstall k10 --namespace=kasten-io`), not `kubectl delete namespace`, which might cause issues with stale services; CRDs remain afterwards and deleting them may lead to data loss [Src: p.865]. The row's actual question — is it safe to uninstall and re-run mid-install, or must the namespace / catalog PVC be cleaned first — is **not answered anywhere in the 9.0.2 guide**, so it ships as `[VERIFY: whether the kasten-io namespace and the catalog persistent volume claim must be removed before re-running helm install after a failed install …; Architect to confirm before a learner is told it is safe]`, with interim guidance to treat a stuck install as a support case with logs attached rather than a retry loop. I will not invent a safe-retry procedure for a first-time implementer on a production cluster.
- **KDO-v2-008 — ADJUSTED.** The immutability toggle was one conditional clause inside a dense five-sentence paragraph; it is now its own paragraph with a bolded lead-in ("Immutability is a toggle, and it belongs on by default"), which is the surfacing the row asked for without inventing a new component. The source claims are unchanged [Src: p.315, p.341]. Two wording changes from the proposal: "Kasten Disaster Recovery" → "Veeam Kasten's own disaster recovery" (naming rule — "K10" and bare "Kasten" product names only appear in verbatim technical strings), and "the first thing a CISO will flag" was kept but paired with "an auditor", plus the practical point that "the bucket was not locked" is a procurement decision you want on the record before the review. Governance framing, no new product claim.

## Minor

- **KBA-v2-003 — ADJUSTED.** The GUI-preferring reassurance is in, trimmed to fit a prerequisite bullet: the setup-wizard-before-a-console parallel, then "Veeam Kasten's dashboard is part of what you are deploying here, so there is no GUI to reach until the platform exists." The proposal's "Veeam Kasten's own dashboard is a product it deploys" read as a product claim about packaging; the rewrite says the same thing about *this deployment*. The bullet list also gained the image-pull item from KVA-v2-001, so this list now carries both round-2 hero asks.
- **KBA-v2-004 — ADJUSTED.** GitOps is now glossed at first use inside the field-tip, in the row's own terms: "treating the install command and its flags as version-controlled files that redeploy the same way every time, the way you might already keep a build script for standing up a new backup server." The proposal's "GitOps here just means" lost the "just" — `just` is a flagged filler word in `Reference/Formatting/glossary.yaml`, and the sentence is stronger without it. The Section 5 mention ("in a GitOps-managed rollout") now sits downstream of the gloss, so the second occurrence is covered in-file.
- **KBA-v2-005 — ADJUSTED.** The Kopia parallel is in as a separate sentence rather than a mid-clause aside: "deduplication, compression and encryption at rest are what a Veeam Backup & Replication backup file already gives you today, with Kopia doing the work here instead of the backup server." I did not use the proposal's "much the same as a VBK file already gives you today" verbatim — VBK encryption is an option you enable in Veeam Backup & Replication rather than an automatic property of the file, and this course cannot cite a Veeam Backup & Replication claim to a Veeam Kasten source, so the sentence stays on the shape of the trio rather than asserting VBR behaviour.
- **KDO-v2-011 — ADJUSTED, and promoted.** A tail clause on the token-auth sentence would still have been a footnote, which is the row's complaint. Instead Multi-Cluster Manager gets its own "Where fleet-wide reporting lives" note at the end of Section 4, grounded in the product's own overview: the Multi-Cluster manager simplifies Veeam Kasten operations across multiple Kubernetes clusters, administrators define primary-secondary relationships, primary instances provide a single entry point and dashboard for managing secondaries, and the secondaries' actions and metrics are summarized in the primary instance [Src: Kasten Docs 9.0.2 p.1012]. The note states the scope boundary explicitly — this course deploys and operates one cluster, and the leadership rollup is the primary instance's dashboard, not the single-cluster one.
- **KVA-v2-010 — ADJUSTED.** The proposal asserted a prerequisite the guide does not state in those terms; there is no network-requirements or firewall section in the 9.0.2 guide to cite. What the guide does say is close enough to carry the row's intent honestly: clusters must have network connectivity to at least one external storage location such as S3, Google Cloud or Azure, and a corresponding location profile must be configured to allow access to it [Src: Kasten Docs 9.0.2 p.474], and the dialog validates the input data on Submit [Src: p.338]. The new paragraph before "What you can point it at" frames the check as the Veeam Data Platform habit it is, and names the symptom — a firewalled endpoint, unresolvable DNS name or closed API port arrives as a failed validation on the profile card, not as an obvious network error.
- **KVA-v2-011 — APPLIED.** A caption under the pod-watch command: `--watch` streams status changes and holds the terminal open rather than returning you to the prompt — that is the command working, not hanging — read until every pod shows Running, then Ctrl+C. Paired with the documented "couple of minutes for all pods to come up" [Src: p.100] so the first-timer knows how long "working" looks like.
- **KVA-v2-012 — APPLIED.** The vSphere case now has a path, not a shape: a new "The vSphere profile, in full" block gives the required fields (vCenter Server as IP or hostname, vSphere User, vSphere Password) [Src: p.123], the precedence rule an implementer needs — install-time vSphere credentials are ignored in favour of the credentials in the Infrastructure Profile [Src: p.123] — and the complete vCenter permission set for a dedicated Veeam Kasten account: Datastore (Allocate space, Browse datastore, Low level file operations), Global (Disable methods, Enable methods, Licenses), Virtual Machine Snapshot Management (Create snapshot, Remove snapshot, Revert to snapshot), Cryptographic operations → Decrypt on 8.0, plus CNS → Searchable for vSphere with Tanzu, assigned on the root vCenter object and the datacenter objects propagating down each subtree [Src: p.124]. That is the request the learner sends their vSphere team.
- **KVA-v2-013 — ADJUSTED; one sub-part deferred with reason.** The hero meta cell now reads "~40 minutes / reading time" rather than "estimated", and a new hero paragraph states that executing the runbook is a separate booking, naming the two things that dominate elapsed time with citations: the deeper CSI check writes data, snapshots it, provisions a new volume from the snapshot and validates the data [Src: p.102], and after the install it may take a couple of minutes for all pods to come up [Src: p.100]. It closes by telling the learner to time the pre-flight pass in their own environment and size the change window from that. **Deferred:** the row's request for a concrete execution-time range (e.g. "45–90 minutes against a healthy cluster"). No such figure exists in the 9.0.2 guide, and elapsed time here is dominated by the customer's storage performance and change-control process — inventing a range would be exactly the kind of plausible-sounding number the pipeline exists to keep out, and planning a change window off a fabricated figure is worse than planning off an honest "time it yourself".

---

## Net change

Lesson 03 stops being a runbook that only works when the network is open and the platform is AWS. The three critical rows all landed on real gaps: an implementer in a restricted network is now told before the install window that an internet-connected install reaches a container registry as well as a chart repository [Src: p.149, p.8] — with a blocking checklist item to gate on — and the module no longer tells them the opposite; the Veeam Repository location profile now shows the Username/Password fields and the Veeam Backup & Replication access-permission prerequisite that the field list was silently missing [Src: p.337, p.338]; and Direct Access is characterised as the lab-only posture it is rather than a granularity preference.

The significant rows closed the "what do I do now" holes: non-AWS platforms have runnable credential paths (Azure and vSphere as exact quoted blocks, GCP and OpenShift as named roles and flags), the ambiguous `Missing deletionPolicy, using default` line has a verdict, the htpasswd step has the string format the guide actually specifies instead of a dead end, and a failed install has a documented first action (collect logs) plus a documented removal path — with the one genuinely unanswered question left as a visible `[VERIFY]` rather than a confident guess. The minor rows closed comprehension and governance gaps: GitOps is glossed, Kopia's trio is anchored to something the learner already owns, the vSphere Infrastructure Profile is complete down to the vCenter privilege list [Src: p.123, p.124], Multi-Cluster Manager is a signpost instead of a footnote [Src: p.1012], destination reachability is a stated pre-check, `--watch` no longer looks like a hang, and the time estimate no longer reads as an execution estimate.

Structure is intact: 9 rail dots, 6 sections plus knowledge check and recap, 7 Continue gates, 5 questions × 3 options with rationales, readiness widget verified in-browser (9 items, 6 blocking, dynamic verdict). Kit components untouched apart from the readiness widget the rows explicitly targeted.

**Gates (final run):** `lint-voice` 0 errors, 8 warnings (identical count and character to the v2 baseline — 5× `repo` inside verbatim `helm repo add` / `repo.example.com` strings, 1× pre-existing `actually`, 2× heuristic false positives on quoted primer output and a table header, 1× lightbox `alt=""`). `smoke-test-html` exit 0. `verify-quotes` 94/94 quotes verbatim in source (was 73/73 — 21 new quoted strings, all verified).

## Handoffs (out-of-file consequences)

1. **Architect — resolve the shipped `[VERIFY]`** in Section 3 ("Before you tear it down"): after a failed or partial Helm install, must the `kasten-io` namespace and the catalog PVC be removed before re-running `helm install`, or is a straight re-run safe? Not addressed in the 9.0.2 guide. If the answer is "safe to re-run", this becomes two sentences and the marker comes out; if not, the cleanup order needs documenting.
2. **Lesson 04 owner — immutability framing.** KDO-v2-008 was raised partly because Module 04 gives immutability strong treatment for the disaster-recovery profile while Module 03 buried it. Module 03 now surfaces it as the standing choice for every production export profile; if Module 04 still reads as though immutability is a Kasten-disaster-recovery-specific control, the two modules should be reconciled in Lesson 04's favour of the general rule.
3. **Lesson 05 owner — Multi-Cluster Manager.** Module 03 now signposts Multi-Cluster Manager as where fleet-wide reporting lives [Src: p.1012] and says the course covers a single cluster. If Module 05 discusses compliance reporting, a one-line callback ("fleet-wide, this is the primary instance's dashboard") would keep the promise this module now makes to the Director persona.
4. **No cross-file edits were made.** This turn touched only `Lesson03_Deploying_and_Configuring_Veeam_Kasten.html` (plus this diff and the log entry).

---
# diff v2 → v3 — Lesson04_Maintaining_Veeam_Kasten_Over_Time.html

Round 2 reviewer rows: `_sources/rows-round2-lesson04.json` (5 rows, all targeting this file — Kasten Backup Admin, Kasten Director x3, Kasten VDP Implementer).
Gates after revision: lint-voice 0 errors / 15 warnings (14 pre-existing + 1 new instance of the same pre-accepted "uncited-claim" heuristic false positive on a PromQL syntax caption, see KBA-v2-002 below) · smoke-test-html exit 0 (info-only: two intentionally-cut brand-chrome components, playwright not installed) · verify-quotes --product K10 46/46 quotes found in source (up from 43/43 at v2; the 3 new verified quotes are `action_ended_total`, `increase`, and two more instances of `k10tools`).

Six knowledge-check questions carried forward unchanged from v2 (Round 1 targeted the count; nothing in Round 2 touches it).

## Row dispositions

- **KBA-v2-002 — APPLIED verbatim.** Section 03 ("Watching Veeam Kasten itself"), first PromQL code block: added the proposed `<p class="caption">` immediately after the query, translating `sum(round(increase(action_ended_total{action="backup",state="succeeded"}[24h])))` clause by clause — metric name, label filter, lookback window, `increase`, then `sum`/`round`. This closes the one place in the module that breaks its own established pattern of translating every command into plain English (every Helm/kubectl block elsewhere gets one). No `[Src:...]` added, per the row's own note: PromQL grammar is generic Prometheus behavior, not a Kasten-specific claim, and it introduces no new product fact — it explains syntax already sitting in the module. I did not extend a matching caption to the second query ("Swap the labels...") — that query reuses the exact grammar just explained, and the existing sentence already states its purpose; a second full breakdown would be redundant with what the learner just read. Mechanical note: `lint-voice` now flags this caption's own "24 hours" as an uncited numerical claim, which is the same heuristic already firing (harmlessly) on the code-block's `<span class="code-block__lang">` label one line above — not a new class of finding, just one more instance of an accepted false positive.

- **KDO-v2-004 — APPLIED verbatim.** Section 02 Pro tip ("Keeping pace with Kubernetes"): appended the ownership sentence — "Put a name on it: agree with the platform team, in writing, who has to sign off before a cluster upgrade proceeds if it would drop the running Veeam Kasten release out of the supported matrix — otherwise the backup platform loses support status by nobody's decision." The module already named the coordination-cost risk (Round 1 grounded it on p.861/862/1098/1099); this row is right that naming a risk without naming an owner leaves a director unable to act on it. No new product fact is asserted — this is governance/process framing, which per the persona's own citation rule needs no `[Src:...]`.

- **KDO-v2-005 — APPLIED verbatim.** Section 03 "Layer three: your existing monitoring stack": appended "In most organizations that central Prometheus/Grafana stack belongs to a platform or observability team rather than the backup team — name who owns wiring the two alerts this module recommends (failed actions, catalog space over 50%) before you assume they already exist." Placed right before the green callout that names those same two alerts, so the forward reference lands immediately rather than several paragraphs later. Organizational-realism framing, not a Kasten product claim — no citation needed.

- **KDO-v2-006 — APPLIED verbatim.** Housekeeping checklist (the row's `loc` calls this a "Remember" note; the exact `orig` text it quotes is actually the housekeeping `<ul class="checklist">` item, not the Section 01 "Remember" box — I matched on the unique `orig` string rather than the location label, since the two disagree and the string is unambiguous). Appended: "Decide who that second person or team is — security, a fellow backup admin, the secrets-management team — and write it into the runbook; 'someone other than you' is a decision your team has to make explicitly, not a default the product supplies." This is the module's single most important resilience control (the catalog cannot be recovered without the passphrase), and the row is right that "someone other than you" can be satisfied on paper without a real name behind it. No new product fact — process framing only.

- **KVA-v2-007 — ADJUSTED (no copy-pasteable install command exists in source; added sourced prose instead of inventing one).** Section 02, first `k10tools` mention (the prose immediately before the `./k10tools primer upgrade` code block — the first `k10tools` invocation in this module, matching one of the two locations the row names). I opened p.866 ("Veeam Kasten Tools") before writing anything: it states the binary is a separate download ("The latest version of k10tools can be found here") with a table of supported OS/architecture combinations (Linux/macOS/Windows), but the "here" is a hyperlink target in the PDF, not printed URL text in the extracted source — I confirmed this by checking the PDF's link objects directly, which is outside the designated text-cache verification method, so I did not reproduce that URL as a quoted string. There is also no curl/wget-style install command for `k10tools` anywhere in the guide (unlike the primer and debug scripts, which are documented as `curl ... | bash` one-liners) — `k10tools` is a manually downloaded binary, not a piped script. Rather than fabricate a plausible-looking install command the source doesn't provide (a hard violation of the quote-verbatim rule), I added a sentence naming the real gap the row identified — that the binary is a separate download, not something Helm installs — and cited p.866 for that fact, without asserting an unverifiable exact command or URL: "get the binary before you need it, since it is a separate download rather than something the Helm chart installs for you: Veeam Kasten publishes `k10tools` for Linux, macOS, and Windows, and the current release is linked from the Veeam Kasten Tools page in the product documentation [Src: Kasten Docs 9.0.2 p.866]." This resolves the learner's actual stall point (where does the executable come from) without inventing a reproducible command that would fail its own verify-quotes gate.

## Net change

All five rows given an explicit disposition; none dropped. Four applied as written (KBA-v2-002, KDO-v2-004, KDO-v2-005, KDO-v2-006) and one adjusted (KVA-v2-007) because the exact artifact the row asked for — a copy-pasteable `k10tools` install command — does not exist verbatim in the 9.0.2 user guide; the underlying gap (learner doesn't know where the binary comes from) is closed with sourced prose instead. No new `[VERIFY]` markers introduced. Substantive changes: the module's one uncaptioned PromQL query now gets the same plain-English treatment every other command in the course gets; two accountability gaps (upgrade-sequencing sign-off, monitoring-stack ownership) and the single most consequential resilience control (the KDR passphrase custodian) now each name a decision the team has to make rather than leaving it implicit; and the first `k10tools` invocation in the module no longer assumes the binary appeared on the operator's machine by magic. Structure is otherwise untouched: five body sections, six knowledge-check questions, every signpost and Continue gate in place, no naming-convention drift (`Veeam Kasten for Kubernetes` / `Veeam Kasten` / `K10` usage unchanged from v2).

---
# diff v2 → v3 — Lesson05_Restores_and_Day_to_Day_Administration.html

Round 2 reviewer rows: `_sources/rows-round2-lesson05.json` (5 rows: 3 Kasten Director, 2 Kasten VDP Implementer — 2 critical, 3 significant).
Order of work: both criticals first, then the two execution-stall rows, then the ownership prompt.
Gates after revision: **lint-voice 0 errors / 9 warnings** (identical set to the v2 baseline — 4 source-verbatim "often" hedges, 1 "actually", 3 uncited-claim heuristic hits on pedagogical prose, 1 empty `alt` on the kit-owned lightbox `<img>`; no new warning introduced by this pass) · **verify-quotes --product K10 37/37** verbatim in source (up from 30/30; every new `<code>` span verified) · **smoke-test-html** pass (same pre-existing component-missing / headless-skipped notices as v2).
Render check: both `.flow` widgets initialise, stage 0 active, detail panel swaps correctly on click for stages 02/04/06, no duplicate element IDs, no horizontal page overflow.

## Row dispositions

- **KDO-v2-001 (critical) — ADJUSTED (answered further than the row asked, then flagged the remaining gap).** Section 04, the four-roles table, `k10-basic` "What it allows" cell. The Director is right: the cell named five capabilities and was silent on deletion, which is the one question a governance conversation opens with. Rather than shipping a bare `[VERIFY]`, I went to the source and found there is a real, citable fact sitting one page past the prose list — the published `k10-basic` ClusterRole definition on p.274 grants `verbs: '*'` on the `policies` resource (apiGroup `config.kio.kasten.io`) and on `restorepoints` (apiGroup `apps.kio.kasten.io`) in the namespaces the role is bound to. So the cell now states both halves: the documented capability list stops short of deletion, *and* the role definition grants all verbs on those two resources `[Src: Kasten Docs 9.0.2 p.274]`. The `[VERIFY]` that follows is narrowed to the genuine residual gap — the capability list and the role definition point different ways, so confirm on your own cluster before answering "who can delete our backups" from this table. That is a better artifact than the proposed question alone: a director gets the documented evidence *and* an honest statement of where the documentation stops, instead of only being told to go ask someone. Kept the reviewer's framing of the stakes; did not adopt the proposal's wording verbatim because it asked me to "state it explicitly" and the source lets me state more than the proposal assumed was available.

- **KDO-v2-002 (critical) — ADJUSTED (upgraded from a question to a sourced answer).** Section 05, the activity-table paragraph. The Director's diagnosis is exactly right — every monitoring layer this course teaches reports on actions and outcomes, never on identity — but the proposed `[VERIFY]` asks the course to "name the screen," and the honest answer is that there is no such screen, which the guide says plainly enough to teach. Two edits: (1) the activity-table paragraph now closes by naming the boundary out loud ("it names the policy that generated an action, not the person who triggered one") and hands off; (2) a new `note--caution` immediately after the alerts paragraph carries the answer, fully cited: Veeam Kasten usage translates into native Kubernetes API calls whichever interface is used, so it can be transparently audited using the Kubernetes Auditing feature with no additional changes `[Src: Kasten Docs 9.0.2 p.818]`; correct user attribution depends on Veeam Kasten being set up with OIDC or token-based authentication, after which dashboard/CLI/API actions are attributed to the initiating user while system actions such as profile or policy validation are attributed to the Veeam Kasten service account `[Src: Kasten Docs 9.0.2 p.819]`; two limits stated before anyone treats it as complete — Kubernetes Auditing only sees Kubernetes API calls, so internal Veeam Kasten events that bypass that API are not logged `[Src: p.819]`, and managed providers (EKS, GKE, AKS) do not allow kube-apiserver flag changes, so audit events land at the metadata level and lose request/response body detail `[Src: p.818]`; and an `AuditConfig` custom resource ships those logs to a cloud object store through a location profile reference `[Src: Kasten Docs 9.0.2 p.621]`. No `[VERIFY]` needed — this is documented behaviour, it was simply in a chapter the course had not touched. One wording fix during drafting: "typically log audit events at the metadata level" (source phrasing) was reworded to "in practice those providers log…" so the hedge survives without tripping the vague-qualifier lint on a word the source itself uses.

- **KVA-v2-008 (significant) — APPLIED (interactive option taken, not the screenshot option).** Section 01. The row is fair and it is the sharpest usability complaint in the set: policy building is the course's stated week-one task, and it was the one hands-on procedure with no rehearsed path — concepts, a scheduling stepper, and a habit-mapping table, but never a walk through the form. Added a six-stage `.flow` chain ("Creating a policy, end to end") placed after the scheduling stepper and before the carry-over table, deliberately matching Module 03's install-flow treatment the reviewer cited, including its *what you should see* beat in every stage: **01 Open the form** — Create New Policy on the Policies page or the Create a Policy menu option on an unmanaged application, the second door pre-populating the policy name `[p.307][p.347]`; **02 Pick the action** — snapshot first, export optional, and stopping here ships a policy with no durable copy `[p.348][p.349]`; **03 Turn on the export** — Enable Backups via Snapshot Exports, the Export Location Profile field and its three destination types, plus the once-saved compatible-profile-only constraint `[p.352][p.363]`; **04 Set the schedule** — the four scheduling components, retention tiers defaulting to the snapshot values, Use a Preset, and no retention schedule at all for on-demand policies `[p.362][p.364][p.383]`; **05 Choose what it protects** — names or labels, wildcard matching, the `*` wildcard, per-application resource filtering `[p.367][p.368][p.346]`; **06 Create it, then watch** — Unmanaged → Non-Compliant → Compliant with "Non-Compliant immediately after saving is the expected reading, not a failure," the action-activity drill-down, and Run Once to prove it now `[p.384][p.385]`. The row offered "interactive **or** screenshot sequence"; I took the interactive route because the screenshot route would require importing new image assets into `images/named/`, and this pass is scoped to one file. Component IDs namespaced (`polStep0…polStep5`, `polFlowDetail`) so they cannot collide with the existing mobility flow's `flowStep*`/`flowDetail` — verified zero duplicate IDs and confirmed both widgets initialise and switch stages in the browser. Stage 04 points back at the stepper rather than restating it, and stage 06 hands forward to "Reading the status your policy produces," so the new widget consolidates rather than duplicates.

- **KVA-v2-009 (significant) — APPLIED (the "state explicitly which of those it is" option, with the answer sourced).** Section 04. The Implementer's complaint is legitimate and the answer turns out to be interesting enough to be worth teaching rather than footnoting: none of the three options in the row is quite right, because group membership is not a thing you do on the cluster at all. Added a new prose paragraph after the `k10-ns-admin` sentence saying so, then giving the two concrete paths: with OIDC, the group name claim identifies the groups a user belongs to, and if the user is in `k10:admins` in the OIDC provider *and* in the same group in the Kubernetes cluster, no additional role bindings are needed because Veeam Kasten creates them at install `[Src: Kasten Docs 9.0.2 p.234]`; on EKS with IAM authentication the equivalent is editing the `aws-auth` ConfigMap so the IAM role maps to the group, which is how the guide's own EKS walkthrough grants a group access `[Src: Kasten Docs 9.0.2 p.260]`. Closed the loop on the dashboard's actual role: bindings are available for subject type Users and Groups `[Src: Kasten Docs 9.0.2 p.280]`, so the dashboard is where you bind a role *to* a group, not where you put people *in* one — which is why a fourth Add Assignment screenshot would have answered the wrong question. Expanded "OIDC" and "EKS" on first use in this module per the define-every-acronym rule.

- **KDO-v2-007 (significant) — ADJUSTED (light wording fit, no citation added).** Section 04, closing paragraph. Applied the substance as proposed: the multi-tenancy handoff now stops to ask who owns rolling the aggregate compliance number up to leadership once application teams own their policies. Two fittings. First, recast from statement to question ("Settle one ownership question before you delegate: does your team still own…?") so it reads as the decision prompt it is rather than an instruction, matching this module's voice where it hands the learner a judgement call. Second, replaced the proposal's "this module does not say" with "the documented guidance sets up the operating model but does not assign that accountability" — more precise about *where* the silence is (the source guidance, not this module's coverage of it), and it avoids a course-referring-to-itself construction the rest of the module does not use. Added the reviewer's own point about timing in plainer terms ("afterwards it becomes an argument rather than a decision"). Deliberately no `[Src:]`: this is a design prompt about accountability, not a product claim, and the p.1092 citation on the preceding clause still anchors the one factual assertion in the sentence (that application teams create and manage their own policies, set schedules, and determine retention).

## Out-of-file handoffs

- **None required for these five rows** — all five land inside this file and were resolved here.
- Recorded for whoever runs the cross-file pass: KDO-v2-002's answer (Kubernetes Auditing, OIDC/token attribution, `AuditConfig`) is now taught in Module 05 only. Module 04's monitoring section and Module 03's authentication section both discuss adjacent ground and would be the natural places to cross-reference it, but neither is in scope for this pass and neither was touched. Likewise the Director's related Direct Access row (KDO-v2-003, "no authentication configured therefore no identity to log against any action") targets Module 03 and is not mine to apply — it is the same accountability thread as KDO-v2-002 and should be applied with it in mind, since Module 05 now sets the expectation that identity attribution exists.

## Net change

All five Round 2 rows applied: 2 applied as proposed in substance (KVA-v2-008, KVA-v2-009 — both taking an option the row itself offered), 3 adjusted (KDO-v2-001, KDO-v2-002, KDO-v2-007), 0 deferred, 0 silently dropped. +62 lines, +1,751 words (824 → 886 lines; 12,652 → 14,403 words), concentrated in the new six-stage policy-creation flow and the audit/attribution note.

The two criticals both moved the module from *raising* a governance question to *answering* it with cited source material — the "who can delete" row gained the published role definition alongside a narrowed `[VERIFY]` (the one place the documentation genuinely contradicts itself), and the "who did it" row gained a complete, four-citation account of Kubernetes-level auditing, attribution prerequisites, and the two limits worth stating before anyone calls it an audit trail. Exactly one `[VERIFY]` marker in the file, and it marks a real documentation conflict rather than unverified drafting.

Structure untouched: five content sections plus knowledge check and recap, all six Continue gates, all eight rail dots, exactly five knowledge-check questions, glossary popover and quiz JSON island unchanged. Naming clean: 0 instances of "Kasten K10" or "Kasten by Veeam", 0 bare "K10" outside `<code>` spans, "Veeam Kasten" throughout. No image assets added, no shared kit file (`course.css`, `components.css`, `components.js`, `index.html`) touched, `_versions/v2/` left alone.

---
## Orchestrator reconciliation (cross-file handoffs)

- **Lesson03 → Lesson04 immutability framing:** checked; Lesson04's immutability mentions are
  KDR-scoped recommendations plus general retirement mechanics and do not present immutability as
  a KDR-only control — consistent with Lesson03's new "standing choice for every production export
  profile" framing. No edit required.
- **Lesson03 → Lesson05 Multi-Cluster callback:** checked; Lesson05 contains no fleet/compliance-
  reporting passage for the callback to attach to. No edit required.
- **Optional cross-references** (Module 04 monitoring / Module 03 authentication pointing to
  Module 05's new audit-attribution passage) → recorded in DEFERRED.md as post-cycle polish rather
  than touching two gate-clean files at close.

## Open [VERIFY] items shipped in v3 (deliberate)

1. **Lesson03 · "Before you tear it down":** whether a failed/partial Helm install requires removing
   the `kasten-io` namespace and catalog PVC before re-running `helm install`, or a straight re-run
   is safe — genuinely not addressed in the 9.0.2 guide; needs a Kasten SME answer.
2. **Lesson05 · roles passage:** one narrowed `[VERIFY]` marking a place where the 9.0.2
   documentation contradicts itself on role capabilities (see Lesson05 diff fragment).
