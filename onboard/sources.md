# Sources (Step 0)

> **PLACEHOLDER** — overwrite by running
> `Pipeline/bin/assemble-sources.py --product <VBR|VONE|VRO|K10> --topic "<topic>" --module <module>`.
> The LXD reads these curated PDF pages + screenshots first and grounds every
> factual claim in them with inline [Src: <doc> p.<N>] citations.
