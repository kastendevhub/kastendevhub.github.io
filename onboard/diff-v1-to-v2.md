# diff v1 → v2 — full course (Round-1 SME review applied)

Date: 2026-08-06. Round 1 reviewers: Editor (ED, 26 rows), Kasten Architect (KA, 14), Senior Kasten SE (KS, 7), Kasten Systems Architect (KSA, 5) — 52 rows total, merged in `review-rows-round1-all.json`, split per file, applied by per-file LXD revision turns. Every row is dispositioned below; nothing was silently dropped.

---
# Diff — index.html v1 → v2 (Round 1)

Source rows: `rows-round1-index.json` (1 row)

## ED-v1-015 — APPLIED

**Loc:** hero lede

**Why applied as written:** The lede introduced the product backwards within a single paragraph — short form ("Veeam Kasten") in sentence one, full form ("Veeam Kasten for Kubernetes") in sentence two — which reads as though two products are being introduced. The naming standard calls for the full name on first use and the short form after; the H1 above the lede does use the full form first, but a reader's true first parse of running prose is the lede, not the heading, so the ordering inside it still needed to match. The Editor's proposed text is the exact two-word-pair swap the diagnosis calls for, with no other change — applied verbatim.

**Before:**
> A self-paced path for the Veeam Data Platform administrator who has been handed Veeam Kasten and a Kubernetes cluster to protect. Veeam Kasten for Kubernetes is an application-centric platform for Kubernetes backup, disaster recovery, and application mobility — this course maps it onto the backup knowledge you already have, then takes you through deployment, ongoing maintenance, restores, and the daily admin routine.

**After:**
> A self-paced path for the Veeam Data Platform administrator who has been handed Veeam Kasten for Kubernetes and a Kubernetes cluster to protect. Veeam Kasten is an application-centric platform for Kubernetes backup, disaster recovery, and application mobility — this course maps it onto the backup knowledge you already have, then takes you through deployment, ongoing maintenance, restores, and the daily admin routine.

## Net change

One sentence-pair swap in the hero lede so the full product name ("Veeam Kasten for Kubernetes") leads and the short form ("Veeam Kasten") follows, per naming standard — no other content, facts, or claims changed.

---
# diff v1 → v2 — Lesson01_From_VDP_Admin_to_Kasten_Admin.html

Round 1 reviewer rows: `_sources/rows-round1-lesson01.json` (7 rows, of which 6 target this file).
Gates after revision: lint-voice 0 errors / 1 warning (pre-existing empty `alt` on the lightbox `<img>`, kit-owned) · smoke-test-html pass · verify-quotes 21/21 quotes found in source.

## Row dispositions

- **ED-v1-005 — APPLIED (both instances).** Section 04, Enterprise card: "obtain a license key from Kasten, or install from cloud marketplaces" → "obtain a license key, or install from cloud marketplaces". Second instance in the same card: "points at the Kasten product page" → "points at the product page". Dropping the vendor rather than substituting keeps both sentences inside what p.3 supports (the source sentence names "Kasten" as the vendor, which is exactly the retired phrasing the glossary bans), and no new factual claim is introduced. Both cited spans untouched.

- **ED-v1-006 — APPLIED.** First compare-card heading: "Starter — also called Veeam Kasten Free" → "Veeam Kasten Free — the Starter edition". This puts the product-branded name first and demotes "Starter" to the descriptor, matching Lesson03 Section 1. Confirmed the reviewer's read that the three downstream "the Starter edition" references (body prose and the "Heads up" note) read correctly once the heading establishes the pairing — no further edits needed, and the p.2 wording ("The default Starter edition… Veeam Kasten Free") supports the pairing in either order.

- **ED-v1-009 — APPLIED.** Knowledge check Q4 replaced wholesale: the block-mode-export item (near-duplicate of Lesson03 Q5) is now an editions item aimed at objective 4, which previously had no assessment while objective 5 had two. Verified both citation anchors against the text cache myself: "functionally the same as the Enterprise edition but limited from a support and scale perspective" and "provided at no charge and intended for evaluation" are p.2; "both editions use the same container images and follow an identical install process" is p.3. The block-mode rule stays taught in Section 05's constraints table and is still assessed indirectly by Q5's Veeam Backup & Replication item, so nothing is lost from the content. Exactly five questions preserved.
  - **Consequential edit (mine, not a row):** the knowledge-check lede said "Five questions on the concept map, the snapshot-and-export distinction, and the Veeam Backup & Replication integration" — it now names the two editions as well, because the signpost has to describe what the check actually asks. Leaving it stale would have created the mismatch the row was fixing.

- **ED-v1-013 — DEFERRED (out of scope).** The row targets Lesson02's hero, not this file. Recorded here only because it arrived in this lesson's rows bundle; it belongs to whoever owns Lesson02. No edit made.

- **KS-v1-003 — APPLIED (verified).** Section 01, first substantive paragraph: Helm v3 now carries an inline gloss — "Helm is Kubernetes' package manager, covered fully in Module 02, so file the term away rather than chasing it now". The reviewer's proposal introduced a new factual claim (Helm = package manager), so I verified it rather than trusting the row: the guide says "the Helm 3 package manager" (p.4, and again on p.118's line). It is general Kubernetes-ecosystem vocabulary rather than a Veeam Kasten product behaviour, so I did not attach a `[Src:]` anchor to the parenthetical — the cited claim it sits inside (p.346) is unchanged and still ends the sentence.

- **ED-v1-016 — APPLIED (plus the tidy the row offered).** Chapter rail dot label "VBR repositories" → "Veeam repositories"; the dot's `aria-label` was already correct and is untouched. Also took the optional cleanup: the Section 05 HTML comment now reads `SECTION 05 — VEEAM BACKUP & REPLICATION INTEGRATION`, so the unexpanded abbreviation appears nowhere in the file, comments included.

- **ED-v1-023 — APPLIED.** Concept map, "Snapshot vs backup" panel: "3am" → "3 a.m.", per the Veeam Spelling and Hyphenation Guide. Worth doing precisely because it sits in the component the module tells the learner to return to.

## Net change

Six of the seven rows applied, one deferred as belonging to another file; five of the six were pure applications and one (KS-v1-003) was applied only after I verified its new factual claim against the 9.0.2 guide. The substantive change is the knowledge check: Q4 stops re-asking Lesson03's block-mode question and now assesses objective 4 (the two editions), which closes the module's only objective-to-assessment gap and removes a course-level duplicate in one move. Everything else is terminology and comprehension polish — vendor-name drift out of Section 04, the edition's branded name leading, an inline gloss so the opening paragraph keeps the hero's promise that every Kubernetes term is defined on the spot or deferred out loud, and "VBR" gone from a rail label that used it before the course ever expands it. Structure is untouched: five sections, all signposts and Continue gates in place, exactly five knowledge-check questions.

---
# diff v1 → v2 — Module 02 (Lesson02_Kubernetes_Foundations_for_Backup_Admins.html)

Round 1 reviewer rows: `_sources/rows-round1-lesson02.json` (15 rows — 2 critical, 3 significant, 10 minor).
Disposition: **11 applied · 4 adjusted · 0 deferred.**

## Critical rows (applied first)

- **ED-v1-001** — **APPLIED.** Lesson → Module across every in-page instance: `<title>`, breadcrumb (`Module 02 of 05`), hero module tag, hero blurb ("This module teaches…", "install against in Module 03"), objectives title ("By the end of this module"), the hero button ("Start the module"), section ledes and prose at Sections 01/06/07/09 ("the rest of this module", "Everything in this module", "the whole subject of this module", "the most important sentence in this module", "Five questions on the ideas this module set up"), the Section 06 annotation callout ("apply it in Module 03"), the Section 07 code-block label ("shell · what Module 03 will run"), the Section 08 callout ("Module 04 picks this up"), and the footer ("Module 2 of 5"). Also caught two instances the row did not enumerate: the sticky-rail dot `data-label`/`aria-label` for the hero ("Module overview" / "Jump to the module overview") and the module-scoped CSS comment. File names, `href`s and the `lesson-nav` CSS class are unchanged — renaming files is out of scope for this row and would break the other five files' links.

- **ED-v1-002** — **APPLIED.** Section 06 "Where it is sharper here" card 03: "a policy decision you will make in **Module 05**." Satisfies **KA-v1-011** (same string, same fix) in one edit.

## Significant rows

- **ED-v1-003** — **ADJUSTED.** Applied the substance: the caption now reads "`k10` appears in lowercase inside the annotation key `k10.kasten.io/is-snapshot-class` because that key is a verbatim technical string" — clearing the last uppercase-`K10`-as-prose in the file and dropping the wrong "technical namespace in the API" gloss. Departed from the proposal on the trailing clause: the proposal ends "the product name is Veeam Kasten for Kubernetes", but the full form is now established earlier on the page (objectives list, per KS-v1-001 / KSA-v1-004), so the naming standard requires the short form here. Kept "the product name is Veeam Kasten."

- **ED-v1-012** + **KA-v1-013** — **RECONCILED into one edit; ED-v1-012 APPLIED, KA-v1-013 ADJUSTED.** The Section 06 "pre-flight check" card is now: heading "The **CSI** pre-flight check is a real test"; body "Run with a specified StorageClass, the primer tool — the pre-flight tool you install and run in Module 03 — runs in a Pod in the cluster and creates a sample application with a persistent volume, writes data to it, snapshots the volume, creates a new volume from that snapshot, and validates the data in the new volume [Src: Kasten Docs 9.0.2 p.102]. Module 03 covers both invocations: the basic run, and this one." This satisfies both rows at once: ED-v1-012's five-word introduction of the undefined "primer tool", and KA-v1-013's scoping of the round-trip claim to the CSI pre-flight run. Adjustments to KA-v1-013 as proposed: its "Lesson 03" wording is normalised to "Module 03" per ED-v1-001, and the two proposals' overlapping sentence openings are merged rather than stacked (applying both verbatim would have produced two competing introductions of the same tool). Verified against the text cache p.102 — "CSI pre-flight checks can be run by deploying the primer tool with a specified StorageClass. This tool runs in a pod in the cluster and performs the following operations: Creates a sample application with a persistent volume and writes some data to it / Takes a snapshot… / Creates a new volume… / Validates the data in the new persistent volume." Citation unchanged; no new factual claim introduced.

- **KSA-v1-001** — **ADJUSTED.** The gap the reviewer named is real and worth closing, but I did not ship the proposal as written: it hangs a `[VERIFY]` marker and a `restricted-v2` reference on a claim I could not confirm — `restricted-v2` does not appear anywhere in the 9.0.2 text cache, and leaving an unverified SCC assertion in a foundations module is worse than the silence it replaces. Instead I verified what the documentation *does* say and asserted only that: "On Red Hat OpenShift specifically, that posture has a name — Security Context Constraints: Veeam Kasten installs customized SecurityContextConstraints (SCC) to ensure that all workloads associated with it have just enough privileges to perform their respective tasks [Src: Kasten Docs 9.0.2 p.60]." Verified against the cache ("Securing Veeam Kasten with SecurityContextConstraints… installs customized SecurityContextConstraints (SCC) to ensure that all workloads associated with Veeam Kasten have just enough privileges to perform their respective tasks"), page computed as 1 + form-feeds before the hit = 60. The expert reader's question now has an answer instead of a caveat, and no `[VERIFY]` debt is carried forward. Whether a *custom* SCC is ever required is left out — the docs do not say so, and this is a concepts module.

## Minor rows

- **ED-v1-018** — **APPLIED.** `<title>Module 02 · Kubernetes Foundations for Backup Admins · Veeam Kasten for Kubernetes Fundamentals</title>` — the Module 03 / Module 05 house pattern. (Aligning the other four files is out of my file scope.)

- **ED-v1-019** — **APPLIED, extended.** The proposal targeted one clause; the row's own rationale asks for one convention across the file, so I applied it to every prose instance: Section 08 body ("runs as Pods among other Pods", "all Veeam Kasten Pods", "watch its Pods reach a running state", "appear in the Pod list"), the `kubectl` caption ("All of the Pods should ultimately display…"), the scope-sorter cards c5 and c6, the knowledge-check rationale for Q5, and the workloads rail dot's `aria-label`. Left lowercase, correctly: `kubectl get pods --namespace kasten-io --watch` (verbatim command) and the visible rail `data-label` strings that are not sentences about the object. This is now internally consistent; Lesson03/Lesson04 remain lowercase and are somebody else's file.

- **ED-v1-022** — **APPLIED.** Section 02 lede: "about **70%** of the way there".

- **ED-v1-024** — **APPLIED** verbatim. Section 04 screenshot `alt` now matches the Module 03 description of the same image file, including the figures a screen-reader user needs. The figcaption is untouched — it teaches this module's own point, which is why the reuse earns its place.

- **ED-v1-026** — **APPLIED.** Hero duration chip: **~45 minutes**.

- **KA-v1-011** — **APPLIED** via the ED-v1-002 edit (identical target string; one edit satisfies both).

- **KS-v1-001** and **KSA-v1-004** — **APPLIED** as a single edit (the two rows propose the same replacement for the same `<li>`). Objectives item 03 now reads "Explain why **Veeam Kasten for Kubernetes** treats a namespace as an application…". Consequence handled: the later Section 08 opener, which previously carried the full form, is now the second use and reads "Veeam Kasten does not sit beside the cluster looking in" — otherwise the fix would have created a naming-standard violation two-thirds down the page instead of removing one. Noted for the panel: the sticky-rail `data-label`/`aria-label` at the top of the DOM still say "Veeam Kasten" (short form) and precede the objectives list; I read terse nav chrome as outside the first-use rule rather than pad every rail dot with the full product name, but flag it in case the Editor disagrees.

- **KSA-v1-002** — **ADJUSTED.** The OpenShift Project gap is worth closing and the placement is right, but I would not ship the proposal's two unverifiable pieces: "adds default quotas and access policy" is a Red Hat platform claim with no support in the Kasten 9.0.2 cache, and the `[VERIFY]` marker asks a question I could answer myself. What the documentation supports is that Veeam Kasten's own OpenShift install instructions treat the two words as interchangeable, so that is what the module now says: "(On Red Hat OpenShift, which this course also targets, the same boundary surfaces as a **Project** — Veeam Kasten's own OpenShift install instructions treat the two words as one thing, telling you to create \"a project (namespace)\" for the installation [Src: Kasten Docs 9.0.2 p.67].)" Verified against the cache — "create a project (namespace) during the operator installation process. By default, the documentation uses the kasten-io namespace" — page computed as 67. The reader gets the terminology bridge without a dangling verification request.

## Deferred

None. Every row is applied or adjusted.

## Net change

v2 closes the parallel-authoring seam that made Module 02 the only unit in the course calling itself a "Lesson", and repairs the two broken forward references (policy construction now points at Module 05, the primer at Module 03 where it is actually introduced). It tightens three factual passages rather than merely rewording them: the pre-flight card is now scoped to the CSI run with a specified StorageClass so it can no longer be read as "the default pre-flight already proved the snapshot path", the OpenShift SCC and Project gaps the systems architect found are closed with claims I verified against the 9.0.2 text cache (p.60, p.67) instead of the proposed `[VERIFY]` placeholders, and the `k10`/`K10` caption now describes the string it actually sits under. Everything else is consistency work — product first-use, Pod capitalisation, the percentage form, the duration chip, and the shared screenshot's alt text — with structure, citations, signposts, Continue gates and the five knowledge-check questions untouched.

---
# diff v1 → v2 — Lesson 03 · Deploying and Configuring Veeam Kasten

Reviewer rows: `_sources/rows-round1-lesson03.json` (14 rows — 1 critical, 6 significant, 7 minor).
Disposition per row below. Applied = proposal taken as written. Adjusted = intent applied, wording or citation fitted. Deferred = not applied, with reason.

## Critical

- **KA-v1-001** — **ADJUSTED.** The row is right that shipping a `snapshot.storage.k8s.io/v1beta1` manifest as the copy-paste artifact is a defect: the group was removed upstream well before Kubernetes 1.29, and 1.29 is where the supported matrix starts [Src: Kasten Docs 9.0.2 p.862]. But the proposal edited the beta block in place — swapping `apiVersion` to `v1` while keeping `driver: hostpath.csi.k8s.io` — which would have produced a `<pre>` block that exists nowhere in the source and fails the verbatim-quote gate. Instead the example was replaced wholesale with the guide's own working `v1` VolumeSnapshotClass, quoted verbatim from the Azure Disk example including its `kubectl get volumesnapshotclass csi-azuredisk-vsc -o yaml` invocation [Src: Kasten Docs 9.0.2 p.105]. The lead-in was rewritten so the sentence no longer promises a "beta API example", and the caption carries the row's reasoning (annotation matters, the group has moved on) plus the row's citations to p.102/p.103 for the Alpha and Beta illustrations and p.862 for the version floor. Learner now copies a manifest that applies on a supported cluster.

## Significant

- **ED-v1-014** — **ADJUSTED.** "Veeam Kasten Backup Server" is indeed not a product object. I checked p.324 and the surrounding Veeam Data Cloud Vault text: the guide never says "Backup Server" here — it says a storage vault is assigned to a *registered cluster* ("assign one or more storage vaults to it from your VDC Account page", the "it" being the registered cluster; the Veeam Kasten instance is what registers with Veeam Data Cloud) [Src: Kasten Docs 9.0.2 p.322–324]. So rather than the row's "this Veeam Kasten instance", the cell now reads "A storage vault assigned to the registered cluster" — the guide's own object, and it no longer implies a Veeam Backup & Replication hop.
- **KA-v1-002** — **ADJUSTED.** The three quoted CSI requirements stay verbatim; the version-context paragraph was added as proposed, with one wording change. The proposal asserted "there is no feature gate left for you to switch on" — a true field statement about upstream Kubernetes, but not a claim the 9.0.2 guide makes, so it would have been an uncited product assertion. Replaced with "every supported release is far past that baseline, so treat those two lines as inherited history rather than work on your list", which rests only on p.102 + p.862. The matching readiness-checklist item (`rdy2`) was updated the same way so the checklist and the prose no longer disagree.
- **KA-v1-003** — **APPLIED.** Blocking item 5 now gates on both the primer run and the `csi -s ${STORAGE_CLASS}` round trip, with the round-trip description cited to p.102. Proposal taken verbatim; the `<code>` fragment is verbatim in source.
- **KA-v1-006** — **APPLIED.** Fifth bullet added and the heading changed to "Five things to know before you choose this route". Verified against p.129: the three accelerating provisioners and the "reading the entire source volume(s), however only changes will be sent to VBR" fallback are both there. Provisioner names kept as `<code>` spans so they stay verbatim.
- **KA-v1-007** — **APPLIED**, with two knock-on edits of my own. Heading is now "The four authentication modes you choose at install" and the paragraph adds the Active Directory/LDAP mode (`--set auth.ldap.enabled=true`, p.250) and the OpenShift OAuth mode (`--set auth.openshift.enabled=true`, p.237) — both flags verified verbatim in the cache. Because the section no longer claims four is the total, I also retuned the Section 4 lede ("match each authentication mode") and recap item 04 ("the authentication modes") so the page doesn't contradict its own heading two screens later.
- **KS-v1-004** — **ADJUSTED.** The intent — don't introduce the immutability toggle as a bare checkbox — is right, and this is the only place in the course where the general-purpose toggle appears. The proposal's benefit clause ("deleted or overwritten by anyone or anything, including ransomware operating with valid credentials") is a paraphrase-plus of p.315, which says nothing of the kind. I sourced the benefit properly instead: the Immutable Backups section states that in a locked bucket restore point data cannot be deleted or modified within a set period even with administrator privileges, and that Veeam Kasten can restore from the immutable copy if an attacker obtains privileged object store credentials [Src: Kasten Docs 9.0.2 p.341]. That claim now sits behind the toggle with its own citation.

## Minor

- **ED-v1-017** — **APPLIED** for this file: footer now reads "Veeam Kasten for Kubernetes Fundamentals · Module 03 of 05 · built against Veeam Kasten 9.0.2". The row's systemic instruction to fix Lessons 02, 04 and 05 is **deferred to the owners of those files** — out of my edit scope this turn (one file only); flagged here so it is not lost.
- **ED-v1-020** — **APPLIED.** "NOT recommended" → "not recommended", and the same paragraph's "a good Data Protection solution" → "a good data protection solution". The Lesson02 l.490 carry-over named in the row is another file's fix — **deferred to Lesson02's owner**.
- **KA-v1-008** — **ADJUSTED.** Citation corrected to p.871, p.872 in both places, and the vintage caveat added. One change: the proposal cited the `v1alpha1` observation to p.872 alone, but the sample's version line ("Valid kubernetes version (v1.17.13)") sits on p.871 and the CSI Capabilities verdict on p.872, so the caveat cites both pages.
- **KA-v1-014** — **APPLIED.** The "Rancher, PKS, and OKD (OpenShift Origin)" clause is gone; the sentence now points at the supported-version matrix and the "no longer actively supported by their vendor or community" note, cited p.861, p.862 (both verified). No dating claim asserted, per the row's own reasoning.
- **KS-v1-005** — **ADJUSTED.** Benefit clause added to the Kopia sentence, but trimmed: the proposal said the data is unreadable "in transit and at rest", and p.315 supports encryption *at rest* only. Kept "unreadable at rest", which is what the source says.
- **KS-v1-006** — **ADJUSTED.** The timing guidance (a pre-first-policy check, not a post-failure diagnosis) is added. The proposal's mechanism claim — that below v1.2.2 the sidecar "is missing capabilities Veeam Kasten depends on to create and clean up snapshots reliably" — is nowhere in the guide, so it was replaced with "the documentation states this as a hard minimum", which is defensible from p.103.
- **KSA-v1-003** — **ADJUSTED, and the [VERIFY] resolved rather than shipped.** The row asked for a `[VERIFY]` on whether Infrastructure Profiles have a declarative path. They do, and the guide shows it: "Create an Infrastructure Profile" applies a `Profile` custom resource (`apiVersion: config.kio.kasten.io/v1alpha1`, `spec.type: Infra`) with `kubectl apply` [Src: Kasten Docs 9.0.2 p.623, p.624]. So the sentence now states the fact with citations instead of carrying an open marker — the asymmetry the row spotted is closed, and no uncertainty is left for a downstream reviewer to chase.

## Net change

Lesson 03 no longer hands a platform team a VolumeSnapshotClass manifest that cannot be applied to any supported cluster — the critical row is resolved with the guide's own `v1` example rather than a hand-edited beta block. Four rows added the version and sizing context an experienced admin needs before committing to an install or an export window (the 1.29 floor against the v1.14.0 baseline, the primer sample's 2020 vintage, the three CSI provisioners that accelerate incremental export to Veeam Backup & Replication, and the AD/LDAP plus OpenShift OAuth modes that exist beyond the install-time picker). Two Senior SE rows turned bare settings into value the learner can repeat back — immutability and Kopia's dedupe/encrypt/compress — with the benefit claims re-sourced to pages that actually carry them, and product-naming, capitalisation, citation and footer-format corrections landed as proposed. All 14 rows dispositioned: 6 applied as written, 8 adjusted, 0 deferred — the only deferrals are the cross-file sub-instructions inside ED-v1-017 and ED-v1-020, which belong to Lessons 02/04/05. Gates: lint-voice 0, smoke-test-html 0, verify-quotes 0 (73/73 quotes verbatim in source).

---
# diff v1 → v2 — Lesson04_Maintaining_Veeam_Kasten_Over_Time.html

Round 1 reviewer rows: `_sources/rows-round1-lesson04.json` (8 rows, all targeting this file).
Gates after revision: lint-voice 0 errors / 14 warnings (all pre-existing: `repo` inside the two verbatim Helm blocks, uncited-claim heuristic hits on headings, objectives, signposts and recap tiles) · smoke-test-html pass · verify-quotes 43/43 quotes found in source.

## Row dispositions

- **ED-v1-007 — ADJUSTED (source wording wins over the proposed standardisation).** Section 03 Compliant card: "its service-level agreements are being respected" → "its service-level agreements (SLAs) are being respected". The row asked me to confirm p.301 and use that single form everywhere; I did, and p.301 reads "Compliant: Objects that both policies apply to and the policy SLAs are being respected" — the source term is SLA, not SLO, so switching to "service-level objectives" would have moved the sentence *away* from its own citation. I kept SLA and expanded it on first (and only) use in this module so the acronym is not left bare. The row's cross-lesson intent still holds, inverted: Lesson02 l.386 and Lesson05 l.219 should standardise on the SLA form, not the SLO form — flagged for whoever owns those two files, since I may not touch them.

- **ED-v1-008 — APPLIED.** Section 03 Removed card: the circular "Objects that have been removed" → "Objects that no longer exist on the cluster; filter to this state to find a removed application and restore it [Src: Kasten Docs 9.0.2 p.301, p.397]". Verified p.397 myself before adding the second anchor: "by default, removed applications are not shown on the Applications page. To discover them, you simply need to filter and select Removed." That is exactly the unasked question the card was leaving open — does a removed application still have restore points — and it now answers it by pointing forward to Module 05's restore path. The identical circularity in Lesson05 l.219 is that file owner's to fix.

- **ED-v1-010 — APPLIED.** Appended the diagnostics-sequence item as the sixth and last object in the quiz JSON array, verbatim from the row. This is the only structural change in the file and it is the one the brief's "keep exactly 5 questions unless a row explicitly targets it" carve-out exists for — the row targets the count directly, and it closes the module's real assessment gap: objective 4 ("Collect the right diagnostics before you open a support case") previously had no item while objective 1 had two. Every citation in the new item was already carried in Section 03 (p.301, p.861, p.862, p.863, p.864), so no new claim entered the module. Note the option strings deliberately use bare `k10_debug_logs.tar.gz` and `k10tools` without `<code>` markup — quiz JSON is rendered as text by the kit, and inline markup would print literally.

- **ED-v1-011 — APPLIED in this file; the two index.html edits DEFERRED to the index owner.** Knowledge-check lede: "Five questions" → "Six questions". The hero meta chip ("5 sections / plus knowledge check") is genuinely unaffected, as the row said. The two remaining places the row names — index.html's Module 04 card chip "5-question check" and its course-wide "Each module closes with a five-question knowledge check" — are outside my file boundary; my recommendation to the index owner is the chip becomes "6-question check" and the course-wide sentence softens to "a short knowledge check", because a hard number there will break again the next time any module gains an item.

- **ED-v1-021 — APPLIED.** Section 01: "releases every 2 weeks" → "releases every two weeks". Style-guide numeral rule, and the surrounding sentence is body prose rather than a measurement. Left "Worth 10 minutes on day one" in Lesson05 alone as the row instructed; nothing in this file needed the same treatment.

- **KA-v1-009 — APPLIED (verified).** Section 04 Veeam Data Platform parallel paragraph: the three-item dependency list re-grounded on the right page and given the guide's own framing — "The guide is blunt about what to retain after enabling KDR, without which restore point catalog recovery will not be possible: the source Cluster ID, the KDR passphrase or the external secret manager details, and the KDR location profile details and credential [Src: Kasten Docs 9.0.2 p.585]." Opened p.585 before applying: the warning block reads "After enabling KDR it is critical to retain the following… The source Cluster ID / The KDR passphrase (or external secret manager details) / The KDR location profile details and credential. Without this information, restore point catalog recovery will not be possible." The reviewer is right that this is a citation a reader will actually follow, and p.571 would have sent them to the enable-and-immutability page instead. Kept "Cluster ID" in the guide's capitalisation because it is the label on the Restore Kasten screen the module screenshots later, so the two now agree.

- **KA-v1-010 — APPLIED (verified).** Section 02, upgrade-primer paragraph: added "— and it requires internet access to `gcr.io`, so on a restricted or air-gapped cluster the System Information page is the route that still works". p.872 states of the primer: "This tool requires Internet access to http://gcr.io". The clause earns its place because the paragraph offers two interchangeable routes to the same answer and, on the estates this course is aimed at, they are not interchangeable — the sentence now tells the reader which one survives an egress restriction. `gcr.io` is marked up as `<code>` (host name, verbatim in source) rather than linked.

- **KA-v1-012 — APPLIED, lightly ADJUSTED for sentence length.** Section 02 Pro tip: the sequencing advice — one of the 12 uncited claims flagged at claims-extraction, and the only one carrying operational advice — is now grounded on p.861/p.862 (the support statement and the version matrix) and p.1098/p.1099 (8.5.9 adding Kubernetes 1.34, then removing 1.30). I verified all four: p.861 carries "Veeam Kasten does not support distribution versions that are no longer actively supported by their respective vendor or community" with the matrix running onto p.862, p.1098 has "Added support for Kubernetes 1.34" under 8.5.9, and p.1099 has "Removed support for Kubernetes 1.30. OpenShift 4.17 clusters continue to be supported" in the same release. The row's replacement was a single very long sentence; I kept its exact clauses and both citation groups and only rebalanced the punctuation so the pro tip still reads as advice rather than as a citation dump.

## Net change

Seven rows applied (one of them, ED-v1-007, applied against its own proposal after the source contradicted it) and no row dropped: two consequential edits sit outside my file boundary and are recorded above as recommendations for the Lesson02/Lesson05 and index.html owners. The substantive changes are the knowledge check gaining a sixth item so objective 4 is finally assessed, the Removed compliance state stopping being a circular definition, and three technical claims getting the citation they should have had — the KDR retention list moved off p.571 onto the p.585 warning that actually states it, the upgrade primer's `gcr.io` dependency named so an air-gapped reader picks the route that works, and the Kubernetes sequencing advice grounded rather than asserted. Structure is otherwise untouched: five sections, every signpost and Continue gate in place, and the only count that changed (five → six questions) changed because a row targeted it.

---
# diff v1 → v2 — Lesson05_Restores_and_Day_to_Day_Administration.html

Round-1 reviewer rows: `_sources/rows-round1-lesson05.json` (7 rows).
Disposition: **applied 5 · adjusted 2 · deferred 0**.

| Row | Reviewer | Disposition | Detail |
|---|---|---|---|
| `ED-v1-004` | Editor | **APPLIED** (extended to the whole file, as the row asks) | US-English sweep, not the one hero sentence only. Changed: hero blurb "centre of gravity" → "center of gravity" (l.47); "All policies centre on" → "center on" (l.83); "Two behaviours worth knowing" → "behaviors" (l.157); "Two behaviours that make an import policy more than a copy" → "behaviors" (h3, l.487); "Practise using it" → "Practice using it" (l.412). Re-grepped the file for the full British-form set (centre/behaviour/organis*/recognis*/summaris*/fulfil*/catalogue/honour/labour/practis*/parameteris*/backwards-compat) — zero remaining hits. Only this file touched; the other four lessons are their own owners' scope. |
| `KA-v1-004` | Kasten Architect | **APPLIED** as written | `k10-ns-admin` completion added to the "bind the administrator role" paragraph. Verified against the text cache before applying: p.267 carries verbatim "In addition to the k10-admin ClusterRole, administrators also require the k10-ns-admin Role for Secret and ConfigMap access within the Veeam Kasten install namespace"; p.268 carries "The k10-ns-admin Role requires a RoleBinding in the release namespace" and "Veeam Kasten creates a RoleBinding for a default Group k10:admins in the Veeam Kasten release namespace." Both citations resolve. `k10-ns-admin` and `k10:admins` are wrapped in `<code>` as verbatim technical strings, which is also the only sanctioned place a `k10-` token appears. No new `<pre>` block added — the `kubectl create rolebinding` form on p.268 is a multi-line continuation block I did not need for this fix, so the existing single-command `<pre>` stands untouched. |
| `KA-v1-005` | Kasten Architect | **APPLIED** as written | Virtual-machine restore claim rescoped. This was one of the three uncited/overreaching claims flagged at claims-extraction, so it took priority. Verified: the sentence "VirtualMachines can only be restored into a new namespace" does sit on p.417, at the end of the PVC/DataVolume-renaming and transform discussion — not in the VM restore section. p.544 has "The process of restoring Virtual Machines backups is similar to restoring any other application using the Veeam Kasten dashboard"; p.545 has the Restore Points page / Virtual Machine application type workflow. The flat "the in-place restore … is not on the menu" sentence is gone; the paragraph now states the restriction in its documented context, cites the workflow section beside it, and routes the residual question to a test restore. |
| `KS-v1-007` | Senior Kasten SE | **ADJUSTED** | Intent applied — the "more flexible permissions model" sentence no longer dead-ends. Kept the closing clause but reworded the reviewer's phrasing: the proposal's "k10-admin's everything and k10-basic's whole namespace" and its "one team owns several applications inside a namespace they don't otherwise control" scenario read as a new behavioural assertion I cannot pin to a page. Shipped instead: "— the middle ground between `k10-admin`'s cluster-wide grant and `k10-basic`'s namespace-wide grant, for when permissions need to follow specific applications rather than a whole cluster or namespace." Every element of that is already carried by the two sentences immediately above it (p.263: ClusterRoleBinding to `k10-admin`; RoleBinding to `k10-basic` in the application's namespace) plus the cited "scopes … to specified applications," so no new citation is needed and none is claimed. |
| `ED-v1-025` | Editor | **APPLIED** (all three instances, as the row asks) | Cross-reference capitalisation: "go back to section 02" → "Section 02" (l.499), "the path section 03 walks" → "Section 03" (l.334), "the three paths in section 02" → "Section 02" (l.664). Matches Lesson01/Lesson03 house style; these are navigable rail components, not generic prose. |
| `KS-v1-002` | Senior Kasten SE | **ADJUSTED** — satisfied jointly with `KSA-v1-005` | Both rows want the full product name on first use, and they name different sentences as "first use." Reconciled into one edit rather than two: the hero objectives list genuinely comes first in reading order, so the full form went there (`KSA-v1-005`, applied verbatim), and the Policies · 01 body sentence this row targets stays at the short form — with a compensating change so the paragraph is not left oddly reduced: the Policies lede one line above, which previously carried "Veeam Kasten for Kubernetes," now reads "Veeam Kasten" too. Net effect: exactly one full-form first use on the page, in the earliest position, short form everywhere after — the naming standard the row is defending, reached the other way round. The row's literal `prop` string is not in the file. |
| `KSA-v1-005` | Kasten Systems Architect | **APPLIED** as written | Hero objective 5 now reads "the built-in Veeam Kasten for Kubernetes roles." This is the page's first substantive product mention, so the full form belongs here. See `KS-v1-002` for the reconciliation. |

## Summary

v2 fixes two real technical defects and closes one demo stall. A team following v1's access section literally would have built a half-privileged administrator — the `k10-ns-admin` Role that `k10-admin` depends on for Secret and ConfigMap access was missing entirely — and v1's flat "virtual machines can only be restored into a new namespace" lifted a PVC-renaming caveat out of context into a DR-plan-changing absolute; both are now stated as the guide states them, with verified page anchors. The remaining changes are editorial hygiene applied file-wide rather than sentence-locally: a US-English sweep (5 instances), capitalised cross-references (3 instances), and a single full-form product first use relocated to the earliest mention on the page. Structure is untouched — 5 sections, 5 Continue gates, 5 knowledge-check questions, all `<pre>` blocks byte-identical to v1.

**Gates after revision:** `lint-voice` exit 0 (3 pre-existing 🟡 uncited-claim advisories on pedagogical framing lines, no 🔴); `smoke-test-html` exit 0 (playwright absent, static checks only); `verify-quotes --product K10` exit 0, 30 quotes verified.

---
## Orchestrator reconciliation (cross-file handoffs)

Four items landed outside their revision turn's file boundary and were applied by the orchestrating LXD after the wave, then re-gated (lint/smoke/quotes all clean):

- **ED-v1-013 — APPLIED to Lesson02** (row was mis-bucketed to Lesson01's bundle by the loc-based split; Lesson01's turn correctly refused it as out-of-scope). Prereqs panel added after Lesson02's hero meta, matching Lesson01/Lesson03's pattern.
- **ED-v1-007 follow-through — APPLIED to Lesson02 (l.386) and Lesson05 (l.219):** compliance-state wording standardized on the source's term — "service-level agreements (SLAs) are being respected" (p.301) — replacing "service levels are being met" / "service-level objectives".
- **ED-v1-008 follow-through — APPLIED to Lesson05 (l.219):** the circular "Removed, meaning objects that are removed" now reads "objects that no longer exist on the cluster — by default they are hidden on the Applications page, so filter to Removed to find and restore them [Src: Kasten Docs 9.0.2 p.301, p.397]", matching Lesson04's fix.
- **ED-v1-011 follow-through — APPLIED to index.html:** Module 04 card chip "5-question check" → "6-question check" (Lesson04's knowledge check gained a sixth item per ED-v1-010), and the course-wide sentence now says "a short knowledge check" so a per-module count change cannot strand it again.

## Deferrals

None outstanding — every Round-1 row is applied, adjusted, or superseded by an equivalent edit as recorded above.
