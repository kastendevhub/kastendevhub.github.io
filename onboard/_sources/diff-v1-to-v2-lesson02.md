# diff v1 → v2 — Module 02 (Lesson02_Kubernetes_Foundations_for_Backup_Admins.html)

Round 1 reviewer rows: `_sources/rows-round1-lesson02.json` (15 rows — 2 critical, 3 significant, 10 minor).
Disposition: **11 applied · 4 adjusted · 0 deferred.**

## Critical rows (applied first)

- **ED-v1-001** — **APPLIED.** Lesson → Module across every in-page instance: `<title>`, breadcrumb (`Module 02 of 05`), hero module tag, hero blurb ("This module teaches…", "install against in Module 03"), objectives title ("By the end of this module"), the hero button ("Start the module"), section ledes and prose at Sections 01/06/07/09 ("the rest of this module", "Everything in this module", "the whole subject of this module", "the most important sentence in this module", "Five questions on the ideas this module set up"), the Section 06 annotation callout ("apply it in Module 03"), the Section 07 code-block label ("shell · what Module 03 will run"), the Section 08 callout ("Module 04 picks this up"), and the footer ("Module 2 of 5"). Also caught two instances the row did not enumerate: the sticky-rail dot `data-label`/`aria-label` for the hero ("Module overview" / "Jump to the module overview") and the module-scoped CSS comment. File names, `href`s and the `lesson-nav` CSS class are unchanged — renaming files is out of scope for this row and would break the other five files' links.

- **ED-v1-002** — **APPLIED.** Section 06 "Where it is sharper here" card 03: "a policy decision you will make in **Module 05**." Satisfies **KA-v1-011** (same string, same fix) in one edit.

## Significant rows

- **ED-v1-003** — **ADJUSTED.** Applied the substance: the caption now reads "`k10` appears in lowercase inside the annotation key `k10.kasten.io/is-snapshot-class` because that key is a verbatim technical string" — clearing the last uppercase-`K10`-as-prose in the file and dropping the wrong "technical namespace in the API" gloss. Departed from the proposal on the trailing clause: the proposal ends "the product name is Veeam Kasten for Kubernetes", but the full form is now established earlier on the page (objectives list, per KS-v1-001 / KSA-v1-004), so the naming standard requires the short form here. Kept "the product name is Veeam Kasten."

- **ED-v1-012** + **KA-v1-013** — **RECONCILED into one edit; ED-v1-012 APPLIED, KA-v1-013 ADJUSTED.** The Section 06 "pre-flight check" card is now: heading "The **CSI** pre-flight check is a real test"; body "Run with a specified StorageClass, the primer tool — the pre-flight tool you install and run in Module 03 — runs in a Pod in the cluster and creates a sample application with a persistent volume, writes data to it, snapshots the volume, creates a new volume from that snapshot, and validates the data in the new volume [Src: Kasten Docs 9.0.2 p.102]. Module 03 covers both invocations: the basic run, and this one." This satisfies both rows at once: ED-v1-012's five-word introduction of the undefined "primer tool", and KA-v1-013's scoping of the round-trip claim to the CSI pre-flight run. Adjustments to KA-v1-013 as proposed: its "Lesson 03" wording is normalised to "Module 03" per ED-v1-001, and the two proposals' overlapping sentence openings are merged rather than stacked (applying both verbatim would have produced two competing introductions of the same tool). Verified against the text cache p.102 — "CSI pre-flight checks can be run by deploying the primer tool with a specified StorageClass. This tool runs in a pod in the cluster and performs the following operations: Creates a sample application with a persistent volume and writes some data to it / Takes a snapshot… / Creates a new volume… / Validates the data in the new persistent volume." Citation unchanged; no new factual claim introduced.

- **KSA-v1-001** — **ADJUSTED.** The gap the reviewer named is real and worth closing, but I did not ship the proposal as written: it hangs a `[VERIFY]` marker and a `restricted-v2` reference on a claim I could not confirm — `restricted-v2` does not appear anywhere in the 9.0.2 text cache, and leaving an unverified SCC assertion in a foundations module is worse than the silence it replaces. Instead I verified what the documentation *does* say and asserted only that: "On Red Hat OpenShift specifically, that posture has a name — Security Context Constraints: Veeam Kasten installs customized SecurityContextConstraints (SCC) to ensure that all workloads associated with it have just enough privileges to perform their respective tasks [Src: Kasten Docs 9.0.2 p.60]." Verified against the cache ("Securing Veeam Kasten with SecurityContextConstraints… installs customized SecurityContextConstraints (SCC) to ensure that all workloads associated with Veeam Kasten have just enough privileges to perform their respective tasks"), page computed as 1 + form-feeds before the hit = 60. The expert reader's question now has an answer instead of a caveat, and no `[VERIFY]` debt is carried forward. Whether a *custom* SCC is ever required is left out — the docs do not say so, and this is a concepts module.

## Minor rows

- **ED-v1-018** — **APPLIED.** `<title>Module 02 · Kubernetes Foundations for Backup Admins · Veeam Kasten for Kubernetes Fundamentals</title>` — the Module 03 / Module 05 house pattern. (Aligning the other four files is out of my file scope.)

- **ED-v1-019** — **APPLIED, extended.** The proposal targeted one clause; the row's own rationale asks for one convention across the file, so I applied it to every prose instance: Section 08 body ("runs as Pods among other Pods", "all Veeam Kasten Pods", "watch its Pods reach a running state", "appear in the Pod list"), the `kubectl` caption ("All of the Pods should ultimately display…"), the scope-sorter cards c5 and c6, the knowledge-check rationale for Q5, and the workloads rail dot's `aria-label`. Left lowercase, correctly: `kubectl get pods --namespace kasten-io --watch` (verbatim command) and the visible rail `data-label` strings that are not sentences about the object. This is now internally consistent; Lesson03/Lesson04 remain lowercase and are somebody else's file.

- **ED-v1-022** — **APPLIED.** Section 02 lede: "about **70%** of the way there".

- **ED-v1-024** — **APPLIED** verbatim. Section 04 screenshot `alt` now matches the Module 03 description of the same image file, including the figures a screen-reader user needs. The figcaption is untouched — it teaches this module's own point, which is why the reuse earns its place.

- **ED-v1-026** — **APPLIED.** Hero duration chip: **~45 minutes**.

- **KA-v1-011** — **APPLIED** via the ED-v1-002 edit (identical target string; one edit satisfies both).

- **KS-v1-001** and **KSA-v1-004** — **APPLIED** as a single edit (the two rows propose the same replacement for the same `<li>`). Objectives item 03 now reads "Explain why **Veeam Kasten for Kubernetes** treats a namespace as an application…". Consequence handled: the later Section 08 opener, which previously carried the full form, is now the second use and reads "Veeam Kasten does not sit beside the cluster looking in" — otherwise the fix would have created a naming-standard violation two-thirds down the page instead of removing one. Noted for the panel: the sticky-rail `data-label`/`aria-label` at the top of the DOM still say "Veeam Kasten" (short form) and precede the objectives list; I read terse nav chrome as outside the first-use rule rather than pad every rail dot with the full product name, but flag it in case the Editor disagrees.

- **KSA-v1-002** — **ADJUSTED.** The OpenShift Project gap is worth closing and the placement is right, but I would not ship the proposal's two unverifiable pieces: "adds default quotas and access policy" is a Red Hat platform claim with no support in the Kasten 9.0.2 cache, and the `[VERIFY]` marker asks a question I could answer myself. What the documentation supports is that Veeam Kasten's own OpenShift install instructions treat the two words as interchangeable, so that is what the module now says: "(On Red Hat OpenShift, which this course also targets, the same boundary surfaces as a **Project** — Veeam Kasten's own OpenShift install instructions treat the two words as one thing, telling you to create \"a project (namespace)\" for the installation [Src: Kasten Docs 9.0.2 p.67].)" Verified against the cache — "create a project (namespace) during the operator installation process. By default, the documentation uses the kasten-io namespace" — page computed as 67. The reader gets the terminology bridge without a dangling verification request.

## Deferred

None. Every row is applied or adjusted.

## Net change

v2 closes the parallel-authoring seam that made Module 02 the only unit in the course calling itself a "Lesson", and repairs the two broken forward references (policy construction now points at Module 05, the primer at Module 03 where it is actually introduced). It tightens three factual passages rather than merely rewording them: the pre-flight card is now scoped to the CSI run with a specified StorageClass so it can no longer be read as "the default pre-flight already proved the snapshot path", the OpenShift SCC and Project gaps the systems architect found are closed with claims I verified against the 9.0.2 text cache (p.60, p.67) instead of the proposed `[VERIFY]` placeholders, and the `k10`/`K10` caption now describes the string it actually sits under. Everything else is consistency work — product first-use, Pod capitalisation, the percentage form, the duration chip, and the shared screenshot's alt text — with structure, citations, signposts, Continue gates and the five knowledge-check questions untouched.
