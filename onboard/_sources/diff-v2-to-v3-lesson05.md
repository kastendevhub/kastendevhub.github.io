# diff v2 → v3 — Lesson05_Restores_and_Day_to_Day_Administration.html

Round 2 reviewer rows: `_sources/rows-round2-lesson05.json` (5 rows: 3 Kasten Director, 2 Kasten VDP Implementer — 2 critical, 3 significant).
Order of work: both criticals first, then the two execution-stall rows, then the ownership prompt.
Gates after revision: **lint-voice 0 errors / 9 warnings** (identical set to the v2 baseline — 4 source-verbatim "often" hedges, 1 "actually", 3 uncited-claim heuristic hits on pedagogical prose, 1 empty `alt` on the kit-owned lightbox `<img>`; no new warning introduced by this pass) · **verify-quotes --product K10 37/37** verbatim in source (up from 30/30; every new `<code>` span verified) · **smoke-test-html** pass (same pre-existing component-missing / headless-skipped notices as v2).
Render check: both `.flow` widgets initialise, stage 0 active, detail panel swaps correctly on click for stages 02/04/06, no duplicate element IDs, no horizontal page overflow.

## Row dispositions

- **KDO-v2-001 (critical) — ADJUSTED (answered further than the row asked, then flagged the remaining gap).** Section 04, the four-roles table, `k10-basic` "What it allows" cell. The Director is right: the cell named five capabilities and was silent on deletion, which is the one question a governance conversation opens with. Rather than shipping a bare `[VERIFY]`, I went to the source and found there is a real, citable fact sitting one page past the prose list — the published `k10-basic` ClusterRole definition on p.274 grants `verbs: '*'` on the `policies` resource (apiGroup `config.kio.kasten.io`) and on `restorepoints` (apiGroup `apps.kio.kasten.io`) in the namespaces the role is bound to. So the cell now states both halves: the documented capability list stops short of deletion, *and* the role definition grants all verbs on those two resources `[Src: Kasten Docs 9.0.2 p.274]`. The `[VERIFY]` that follows is narrowed to the genuine residual gap — the capability list and the role definition point different ways, so confirm on your own cluster before answering "who can delete our backups" from this table. That is a better artifact than the proposed question alone: a director gets the documented evidence *and* an honest statement of where the documentation stops, instead of only being told to go ask someone. Kept the reviewer's framing of the stakes; did not adopt the proposal's wording verbatim because it asked me to "state it explicitly" and the source lets me state more than the proposal assumed was available.

- **KDO-v2-002 (critical) — ADJUSTED (upgraded from a question to a sourced answer).** Section 05, the activity-table paragraph. The Director's diagnosis is exactly right — every monitoring layer this course teaches reports on actions and outcomes, never on identity — but the proposed `[VERIFY]` asks the course to "name the screen," and the honest answer is that there is no such screen, which the guide says plainly enough to teach. Two edits: (1) the activity-table paragraph now closes by naming the boundary out loud ("it names the policy that generated an action, not the person who triggered one") and hands off; (2) a new `note--caution` immediately after the alerts paragraph carries the answer, fully cited: Veeam Kasten usage translates into native Kubernetes API calls whichever interface is used, so it can be transparently audited using the Kubernetes Auditing feature with no additional changes `[Src: Kasten Docs 9.0.2 p.818]`; correct user attribution depends on Veeam Kasten being set up with OIDC or token-based authentication, after which dashboard/CLI/API actions are attributed to the initiating user while system actions such as profile or policy validation are attributed to the Veeam Kasten service account `[Src: Kasten Docs 9.0.2 p.819]`; two limits stated before anyone treats it as complete — Kubernetes Auditing only sees Kubernetes API calls, so internal Veeam Kasten events that bypass that API are not logged `[Src: p.819]`, and managed providers (EKS, GKE, AKS) do not allow kube-apiserver flag changes, so audit events land at the metadata level and lose request/response body detail `[Src: p.818]`; and an `AuditConfig` custom resource ships those logs to a cloud object store through a location profile reference `[Src: Kasten Docs 9.0.2 p.621]`. No `[VERIFY]` needed — this is documented behaviour, it was simply in a chapter the course had not touched. One wording fix during drafting: "typically log audit events at the metadata level" (source phrasing) was reworded to "in practice those providers log…" so the hedge survives without tripping the vague-qualifier lint on a word the source itself uses.

- **KVA-v2-008 (significant) — APPLIED (interactive option taken, not the screenshot option).** Section 01. The row is fair and it is the sharpest usability complaint in the set: policy building is the course's stated week-one task, and it was the one hands-on procedure with no rehearsed path — concepts, a scheduling stepper, and a habit-mapping table, but never a walk through the form. Added a six-stage `.flow` chain ("Creating a policy, end to end") placed after the scheduling stepper and before the carry-over table, deliberately matching Module 03's install-flow treatment the reviewer cited, including its *what you should see* beat in every stage: **01 Open the form** — Create New Policy on the Policies page or the Create a Policy menu option on an unmanaged application, the second door pre-populating the policy name `[p.307][p.347]`; **02 Pick the action** — snapshot first, export optional, and stopping here ships a policy with no durable copy `[p.348][p.349]`; **03 Turn on the export** — Enable Backups via Snapshot Exports, the Export Location Profile field and its three destination types, plus the once-saved compatible-profile-only constraint `[p.352][p.363]`; **04 Set the schedule** — the four scheduling components, retention tiers defaulting to the snapshot values, Use a Preset, and no retention schedule at all for on-demand policies `[p.362][p.364][p.383]`; **05 Choose what it protects** — names or labels, wildcard matching, the `*` wildcard, per-application resource filtering `[p.367][p.368][p.346]`; **06 Create it, then watch** — Unmanaged → Non-Compliant → Compliant with "Non-Compliant immediately after saving is the expected reading, not a failure," the action-activity drill-down, and Run Once to prove it now `[p.384][p.385]`. The row offered "interactive **or** screenshot sequence"; I took the interactive route because the screenshot route would require importing new image assets into `images/named/`, and this pass is scoped to one file. Component IDs namespaced (`polStep0…polStep5`, `polFlowDetail`) so they cannot collide with the existing mobility flow's `flowStep*`/`flowDetail` — verified zero duplicate IDs and confirmed both widgets initialise and switch stages in the browser. Stage 04 points back at the stepper rather than restating it, and stage 06 hands forward to "Reading the status your policy produces," so the new widget consolidates rather than duplicates.

- **KVA-v2-009 (significant) — APPLIED (the "state explicitly which of those it is" option, with the answer sourced).** Section 04. The Implementer's complaint is legitimate and the answer turns out to be interesting enough to be worth teaching rather than footnoting: none of the three options in the row is quite right, because group membership is not a thing you do on the cluster at all. Added a new prose paragraph after the `k10-ns-admin` sentence saying so, then giving the two concrete paths: with OIDC, the group name claim identifies the groups a user belongs to, and if the user is in `k10:admins` in the OIDC provider *and* in the same group in the Kubernetes cluster, no additional role bindings are needed because Veeam Kasten creates them at install `[Src: Kasten Docs 9.0.2 p.234]`; on EKS with IAM authentication the equivalent is editing the `aws-auth` ConfigMap so the IAM role maps to the group, which is how the guide's own EKS walkthrough grants a group access `[Src: Kasten Docs 9.0.2 p.260]`. Closed the loop on the dashboard's actual role: bindings are available for subject type Users and Groups `[Src: Kasten Docs 9.0.2 p.280]`, so the dashboard is where you bind a role *to* a group, not where you put people *in* one — which is why a fourth Add Assignment screenshot would have answered the wrong question. Expanded "OIDC" and "EKS" on first use in this module per the define-every-acronym rule.

- **KDO-v2-007 (significant) — ADJUSTED (light wording fit, no citation added).** Section 04, closing paragraph. Applied the substance as proposed: the multi-tenancy handoff now stops to ask who owns rolling the aggregate compliance number up to leadership once application teams own their policies. Two fittings. First, recast from statement to question ("Settle one ownership question before you delegate: does your team still own…?") so it reads as the decision prompt it is rather than an instruction, matching this module's voice where it hands the learner a judgement call. Second, replaced the proposal's "this module does not say" with "the documented guidance sets up the operating model but does not assign that accountability" — more precise about *where* the silence is (the source guidance, not this module's coverage of it), and it avoids a course-referring-to-itself construction the rest of the module does not use. Added the reviewer's own point about timing in plainer terms ("afterwards it becomes an argument rather than a decision"). Deliberately no `[Src:]`: this is a design prompt about accountability, not a product claim, and the p.1092 citation on the preceding clause still anchors the one factual assertion in the sentence (that application teams create and manage their own policies, set schedules, and determine retention).

## Out-of-file handoffs

- **None required for these five rows** — all five land inside this file and were resolved here.
- Recorded for whoever runs the cross-file pass: KDO-v2-002's answer (Kubernetes Auditing, OIDC/token attribution, `AuditConfig`) is now taught in Module 05 only. Module 04's monitoring section and Module 03's authentication section both discuss adjacent ground and would be the natural places to cross-reference it, but neither is in scope for this pass and neither was touched. Likewise the Director's related Direct Access row (KDO-v2-003, "no authentication configured therefore no identity to log against any action") targets Module 03 and is not mine to apply — it is the same accountability thread as KDO-v2-002 and should be applied with it in mind, since Module 05 now sets the expectation that identity attribution exists.

## Net change

All five Round 2 rows applied: 2 applied as proposed in substance (KVA-v2-008, KVA-v2-009 — both taking an option the row itself offered), 3 adjusted (KDO-v2-001, KDO-v2-002, KDO-v2-007), 0 deferred, 0 silently dropped. +62 lines, +1,751 words (824 → 886 lines; 12,652 → 14,403 words), concentrated in the new six-stage policy-creation flow and the audit/attribution note.

The two criticals both moved the module from *raising* a governance question to *answering* it with cited source material — the "who can delete" row gained the published role definition alongside a narrowed `[VERIFY]` (the one place the documentation genuinely contradicts itself), and the "who did it" row gained a complete, four-citation account of Kubernetes-level auditing, attribution prerequisites, and the two limits worth stating before anyone calls it an audit trail. Exactly one `[VERIFY]` marker in the file, and it marks a real documentation conflict rather than unverified drafting.

Structure untouched: five content sections plus knowledge check and recap, all six Continue gates, all eight rail dots, exactly five knowledge-check questions, glossary popover and quiz JSON island unchanged. Naming clean: 0 instances of "Kasten K10" or "Kasten by Veeam", 0 bare "K10" outside `<code>` spans, "Veeam Kasten" throughout. No image assets added, no shared kit file (`course.css`, `components.css`, `components.js`, `index.html`) touched, `_versions/v2/` left alone.
