# diff v2 → v3 — Lesson04_Maintaining_Veeam_Kasten_Over_Time.html

Round 2 reviewer rows: `_sources/rows-round2-lesson04.json` (5 rows, all targeting this file — Kasten Backup Admin, Kasten Director x3, Kasten VDP Implementer).
Gates after revision: lint-voice 0 errors / 15 warnings (14 pre-existing + 1 new instance of the same pre-accepted "uncited-claim" heuristic false positive on a PromQL syntax caption, see KBA-v2-002 below) · smoke-test-html exit 0 (info-only: two intentionally-cut brand-chrome components, playwright not installed) · verify-quotes --product K10 46/46 quotes found in source (up from 43/43 at v2; the 3 new verified quotes are `action_ended_total`, `increase`, and two more instances of `k10tools`).

Six knowledge-check questions carried forward unchanged from v2 (Round 1 targeted the count; nothing in Round 2 touches it).

## Row dispositions

- **KBA-v2-002 — APPLIED verbatim.** Section 03 ("Watching Veeam Kasten itself"), first PromQL code block: added the proposed `<p class="caption">` immediately after the query, translating `sum(round(increase(action_ended_total{action="backup",state="succeeded"}[24h])))` clause by clause — metric name, label filter, lookback window, `increase`, then `sum`/`round`. This closes the one place in the module that breaks its own established pattern of translating every command into plain English (every Helm/kubectl block elsewhere gets one). No `[Src:...]` added, per the row's own note: PromQL grammar is generic Prometheus behavior, not a Kasten-specific claim, and it introduces no new product fact — it explains syntax already sitting in the module. I did not extend a matching caption to the second query ("Swap the labels...") — that query reuses the exact grammar just explained, and the existing sentence already states its purpose; a second full breakdown would be redundant with what the learner just read. Mechanical note: `lint-voice` now flags this caption's own "24 hours" as an uncited numerical claim, which is the same heuristic already firing (harmlessly) on the code-block's `<span class="code-block__lang">` label one line above — not a new class of finding, just one more instance of an accepted false positive.

- **KDO-v2-004 — APPLIED verbatim.** Section 02 Pro tip ("Keeping pace with Kubernetes"): appended the ownership sentence — "Put a name on it: agree with the platform team, in writing, who has to sign off before a cluster upgrade proceeds if it would drop the running Veeam Kasten release out of the supported matrix — otherwise the backup platform loses support status by nobody's decision." The module already named the coordination-cost risk (Round 1 grounded it on p.861/862/1098/1099); this row is right that naming a risk without naming an owner leaves a director unable to act on it. No new product fact is asserted — this is governance/process framing, which per the persona's own citation rule needs no `[Src:...]`.

- **KDO-v2-005 — APPLIED verbatim.** Section 03 "Layer three: your existing monitoring stack": appended "In most organizations that central Prometheus/Grafana stack belongs to a platform or observability team rather than the backup team — name who owns wiring the two alerts this module recommends (failed actions, catalog space over 50%) before you assume they already exist." Placed right before the green callout that names those same two alerts, so the forward reference lands immediately rather than several paragraphs later. Organizational-realism framing, not a Kasten product claim — no citation needed.

- **KDO-v2-006 — APPLIED verbatim.** Housekeeping checklist (the row's `loc` calls this a "Remember" note; the exact `orig` text it quotes is actually the housekeeping `<ul class="checklist">` item, not the Section 01 "Remember" box — I matched on the unique `orig` string rather than the location label, since the two disagree and the string is unambiguous). Appended: "Decide who that second person or team is — security, a fellow backup admin, the secrets-management team — and write it into the runbook; 'someone other than you' is a decision your team has to make explicitly, not a default the product supplies." This is the module's single most important resilience control (the catalog cannot be recovered without the passphrase), and the row is right that "someone other than you" can be satisfied on paper without a real name behind it. No new product fact — process framing only.

- **KVA-v2-007 — ADJUSTED (no copy-pasteable install command exists in source; added sourced prose instead of inventing one).** Section 02, first `k10tools` mention (the prose immediately before the `./k10tools primer upgrade` code block — the first `k10tools` invocation in this module, matching one of the two locations the row names). I opened p.866 ("Veeam Kasten Tools") before writing anything: it states the binary is a separate download ("The latest version of k10tools can be found here") with a table of supported OS/architecture combinations (Linux/macOS/Windows), but the "here" is a hyperlink target in the PDF, not printed URL text in the extracted source — I confirmed this by checking the PDF's link objects directly, which is outside the designated text-cache verification method, so I did not reproduce that URL as a quoted string. There is also no curl/wget-style install command for `k10tools` anywhere in the guide (unlike the primer and debug scripts, which are documented as `curl ... | bash` one-liners) — `k10tools` is a manually downloaded binary, not a piped script. Rather than fabricate a plausible-looking install command the source doesn't provide (a hard violation of the quote-verbatim rule), I added a sentence naming the real gap the row identified — that the binary is a separate download, not something Helm installs — and cited p.866 for that fact, without asserting an unverifiable exact command or URL: "get the binary before you need it, since it is a separate download rather than something the Helm chart installs for you: Veeam Kasten publishes `k10tools` for Linux, macOS, and Windows, and the current release is linked from the Veeam Kasten Tools page in the product documentation [Src: Kasten Docs 9.0.2 p.866]." This resolves the learner's actual stall point (where does the executable come from) without inventing a reproducible command that would fail its own verify-quotes gate.

## Net change

All five rows given an explicit disposition; none dropped. Four applied as written (KBA-v2-002, KDO-v2-004, KDO-v2-005, KDO-v2-006) and one adjusted (KVA-v2-007) because the exact artifact the row asked for — a copy-pasteable `k10tools` install command — does not exist verbatim in the 9.0.2 user guide; the underlying gap (learner doesn't know where the binary comes from) is closed with sourced prose instead. No new `[VERIFY]` markers introduced. Substantive changes: the module's one uncaptioned PromQL query now gets the same plain-English treatment every other command in the course gets; two accountability gaps (upgrade-sequencing sign-off, monitoring-stack ownership) and the single most consequential resilience control (the KDR passphrase custodian) now each name a decision the team has to make rather than leaving it implicit; and the first `k10tools` invocation in the module no longer assumes the binary appeared on the operator's machine by magic. Structure is otherwise untouched: five body sections, six knowledge-check questions, every signpost and Continue gate in place, no naming-convention drift (`Veeam Kasten for Kubernetes` / `Veeam Kasten` / `K10` usage unchanged from v2).
